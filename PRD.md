# 认知年轮（Cognitive Rings）产品需求文档

| 项 | 值 |
| :--- | :--- |
| 版本 | v1.0 |
| 日期 | 2026-08-29 |
| 赛事 | 知乎黑客松 2026 校园新锐季 |
| 开发窗口 | 2026-09-13 10:00 → 09-15 10:00（48h） |
| 路演 | 2026-09-19 北京 798 |
| 产品名 | 认知年轮 / Cognitive Rings（统一使用，不再混用「认知天文台」「十年共识漂移计」） |

---

## 1. 背景与机会

现在所有 AI 搜索产品都在回答同一个问题：「今天什么是对的。」它们把知乎当成语料库,抽取结论、丢弃时间。

但知乎最不可替代的资产恰恰在时间维度上：

- **跨周期的严肃长文**。同一个议题下有 2014 年、2018 年、2022 年、2026 年的回答，作者在不同信息条件下做出不同判断，且都留在原地没有被删除。微博没有 10 年前的严肃长文，小红书没有跨周期的行业复盘。
- **公开反驳最高票的文化**。知乎答主写反共识观点有固定句式：「反对最高票」「不同意排名第一」「等几年再来看这个答案」「利益相关，我做这行」。这是一套**可检索的对抗结构**，其他任何中文平台都不具备。
- **答主身份可验证**。一线从业者、高校科研、行业分析师的认证身份，让「谁在什么位置上说了什么」变得可分析。

机会点：**这三样东西加起来，可以重建一个议题上人类认知的演化史，而这件事只能在知乎做。**

## 2. 产品定义与定位

**认知年轮**是一个议题级的共识演化分析控制台。输入一个议题，输出该议题在知乎上 10 余年的完整认知轨迹：主流判断如何形成、被什么变量推翻、当年的少数派在哪里、今天哪些低热度非共识值得押注。

产品形态是**数据可视化控制台，不含 Chat 输入框**。理由：
1. 确定性演示 —— 路演现场不能出现「模型这次答得不太好」。
2. 配额可控 —— 自由追问会让直答 API 在现场几分钟内耗尽。
3. 差异化 —— 有输入框就会被判定为「又一个 AI 问答壳」，直接丢掉创新度分。

**一句话定位**：知乎专属的共识考古引擎 —— 不告诉你答案，告诉你答案是怎么变的。

## 3. 目标用户与核心场景

| 用户 | 场景 | 完成后得到什么 |
| :--- | :--- | :--- |
| 行业新人 / 转行者 | 进入一个陌生领域，网上说法互相矛盾，不知道哪些是过期结论 | 分清「已被推翻的旧共识」和「仍然成立的判断」，避免拿 5 年前的结论做今天的决策 |
| 投研 / 战略分析 | 判断一个赛道的共识是否已经过热 | 看到历史上共识被推翻的因果路径，识别当下的「伪共识」 |
| 老知乎用户 | 好奇自己当年的判断对不对 | 认知年轮测试 + 体检单，社交传播动机 |
| 内容创作者 | 找选题 | 「今日候选先知」直接给出被低估的观点 |

**核心闭环**：选议题 → 看散点图（谁对谁错一目了然）→ 点开证据卡（原帖可回溯）→ 理解条件归因（什么变量推翻了共识）→ 做认知年轮测试（我在哪一年）→ 看今日候选先知（下一个变量是什么）→ 分享挑战链接。

## 4. 核心叙事框架：「条件变了」而不是「打脸」

这是产品的价值观，也是最重要的防御设计。

❌ 不用的表述：「万赞幻觉」「顶流被打脸」「你落后 3 个代际」
✅ 使用的表述：「当年的合理判断」「后来变化的变量」「你和哪一年的知乎同频」

**为什么**：
1. **对答主的尊重是硬要求**。评委是知乎自己人，创作者生态是他们最核心的资产。一个公开羞辱高赞答主的产品不可能拿高分。
2. **「条件变了」在逻辑上更正确**。2016 年「三元锂五年内无法解决热失控，纯电必然是过渡」这个判断，在 2016 年的信息条件下是成立的 —— 它的隐含前提是「电池封装维度不变、成本曲线线性下降」。2020 年 CTP / 刀片电池动了封装这个变量，结论才失效。答主不是蠢，是世界变了。
3. **这才产生可迁移的价值**。产品真正的输出不是「谁错了」，而是**共识最常被哪类变量推翻**：成本曲线、政策转向、工程封装创新、算力阶跃、供应链重构。这是一套识别当下伪共识的思维框架，也是对「后见之明偏差」质询最硬的回答。

戏剧性一点没少，姿态是尊重的。

## 5. 信息架构

单页控制台，四个区块自上而下：

```
+----------------------------------------------------------------------------------+
|  议题选择器  [新能源汽车] [大模型能力上限] [元宇宙] [生化环材] ... + 自由输入      |
|  状态条：● 黄金议题（已人工核验，0 延迟）  /  ○ 冷启动推演中（约 25s）             |
+----------------------------------------------------------------------------------+
|  【一】共识散点图 Consensus Scatter                              [主视觉]         |
|                                                                                  |
|   激进 ↑                                                                         |
|        │        ●大红          ●大红                                             |
|        │   ●大红         ●中黄        ○小绿                                       |
|   中立 ┼────────────────────────────────────────────────────────                 |
|        │    ○小绿   ○小绿        ●中绿        ●大绿                              |
|   保守 ↓        ○小绿(先知)                                                      |
|        2014      2017      2020      2023      2026                              |
|                                                                                  |
|   点大小 = 赞数   ● 红 = 后被证伪  ● 绿 = 后被验证  ● 黄 = 未决                    |
|   ▸ 本议题样本 N=14：万赞档事后正确率 32%，500 赞以下长回答 61%                    |
+----------------------------------------------------------------------------------+
|  【二】证据卡（点击散点展开）                                                     |
|  +--------------------------------+--------------------------------+             |
|  | 当年主流判断  2016-04          | 当年少数派  2017-11            |             |
|  | 某车企动力工程师 · 18.2k 赞    | 电池产线工程师 · 340 赞        |             |
|  | 「原文逐字引用……」             | 「原文逐字引用……」             |             |
|  | 🔗 查看原帖                    | 🔗 查看原帖                    |             |
|  +--------------------------------+--------------------------------+             |
|  【三】条件归因：这个共识被什么推翻了                                             |
|   隐含前提：封装维度固定 ──✕── 2020 CTP/刀片 ──▸ 体积利用率 +50%                  |
|   变量类型：⚙️ 工程封装创新（本议题历史上 3 次共识翻转中出现 2 次）                |
+----------------------------------------------------------------------------------+
|  【四】今日候选先知（待验证）        |  认知年轮测试                              |
|  ○ 340 赞 · 固态电池产线工程师       |  当下终极争议题 → 你的直觉                  |
|    「隔膜良率才是真瓶颈」            |  → 「你和 2023 年 84% 的知乎共识同频」      |
|    [押注] [提醒我 2031 年回来看]     |  → [生成体检单] [发起挑战链接]              |
+----------------------------------------------------------------------------------+
```

视觉：暗色底 + 知乎蓝主色，年轮放射纹理作为背景水印。

---

## 6. 功能需求

### F1 · 共识散点图（P0，主视觉）

**目的**：一屏之内让观众自己看出「谁对谁错」，不需要旁白。这是相比原方案「堆叠面积图 + 双卡片」的关键升级 —— 面积图只能讲声量消长，讲不出对错。

**规格**
- X 轴：回答发布时间（连续，非分桶）
- Y 轴：立场光谱得分 `stance ∈ [-1, 1]`，由模型标注，两极标签按议题动态生成（如「纯电是过渡 ↔ 纯电是终局」）
- 点大小：赞数（对数映射，避免万赞点糊掉全图）
- 点颜色：`verdict` — `falsified` 红 / `verified` 绿 / `open` 黄
- 悬停：显示答主身份 + 赞数 + 一句摘要
- 点击：下方证据卡区域展开该点详情

**衍生指标（P0，传播抓手）**
在图下方给一行硬指标：`本议题样本 N=14：万赞档事后正确率 32%，500 赞以下长回答 61%`。这是最容易被截图转发的东西。

⚠️ 措辞纪律：只说「本议题 N 条样本」，**绝不号称统计显著**。N=14 不具备统计效力，如果被质询，标准回答见 §12 Q4。

**验收标准**
- 15 个黄金议题每个至少 10 个点、覆盖 3 个以上年份、同时存在红点和绿点
- 万赞红点出现在图上半区、低赞绿点出现在下半区的视觉规律成立（若数据不成立就换议题，不修数据）
- 点击任意点 200ms 内展开证据卡

### F2 · 证据卡（P0）

**目的**：可信度的载体。评委只要点开一个链接发现回答不存在，整个 Demo 当场死亡。

**规格**
- 双栏对比：「当年主流判断」vs「当年少数派」
- 每张卡片字段：答主展示名 + 认证身份、发布时间、赞数、引文、原帖链接
- 引文必须是原文逐字子串，禁止润色、禁止省略号拼接不连续片段
- 每张卡片右下角常驻「🔗 查看原帖」，新窗口打开

**关键设计：少数派 ≠ 低赞**
不再用「7 赞地下先知」定义少数派，改用**立场对立**定义。当年一条 800 赞的反共识回答，和一条 9 赞的相比，论证力完全相同（对比的是立场不是赞数），但检索可达性天差地别。详见 §8 检索管线。

**验收标准**
- 100% 卡片引文通过 `quote in source_text` 本地校验
- 100% 卡片链接可点开且指向真实存在的回答
- 15 个黄金议题的全部卡片经人工逐条点开核验（见 §10）

### F3 · 条件归因（P0）

**目的**：把「打脸」转化为「条件变了」，产出可迁移的思维模型。这是 AI 场景价值 40% 权重的主要得分点。

**规格**
每次共识翻转输出一个结构：
- `implicit_premise`：旧共识的隐含前提（如「电池封装维度固定」）
- `changed_variable`：实际发生变化的变量（如「2020 CTP/刀片改变封装维度」）
- `variable_type`：变量归类，枚举 —— `cost_curve` 成本曲线 / `policy` 政策转向 / `engineering` 工程创新 / `compute` 算力阶跃 / `supply_chain` 供应链重构 / `demand_shift` 需求迁移
- 议题级汇总：「本议题历史上 3 次共识翻转中，工程创新出现 2 次」

**验收标准**：每个黄金议题至少 2 次翻转有完整归因；`variable_type` 汇总在 UI 上可见。

### F4 · 今日候选先知 + 时间胶囊（P0，创新度核心）

**目的**：这是对「你不就是后见之明吗」这一质询唯一有力的回答 —— 不靠话术，靠功能。同时给用户回访理由。

**规格**
- 用与历史检索完全相同的规则，从近 6 个月回答中捞出 3 条：低热度 + 认证身份 + 反当前主流立场 + 长文
- 全部标注 `verdict: open`（待验证），在散点图上显示为黄点，位于时间轴最右端
- 每条支持两个动作：
  - **押注** —— 用户选「我认为这条会被验证 / 不会」，本地记票，展示当前分布
  - **时间胶囊** —— 生成 `/capsule/{id}` 永久 URL，含用户留言 + 当前分布快照 + 目标回看年份
- 时间胶囊页面可直接分享，是天然的传播物料

**路演台词**：「我们不是事后诸葛亮 —— 这是我们今天押的注，欢迎五年后来查我们的账。」

**验收标准**：3 个黄金议题有今日候选先知；时间胶囊 URL 刷新后仍可访问（落库，非 localStorage）。

### F5 · 认知年轮测试（P0，互动 + 传播）

**目的**：把观众/评委拉进来，产出分享物料。

⚠️ **原方案的重大风险已修正**。原设计假设评委会选中「2023 年的旧答案」然后被打脸，这是在赌博：知乎的 AI 方向评委很可能直接选中前沿选项，笑点当场崩掉；而且「您的认知停留在 3 年前」对着评委说有羞辱风险。

**修正后规格：每个选项都有好结果**

| 用户选择 | 系统输出 |
| :--- | :--- |
| 命中 2026 前沿 | 「你和 2026 年知乎前沿同频 —— 这个判断在历史回答中只有 3% 提前说出来过」 |
| 命中 2023 共识 | 「你和 2023 年 84% 的知乎共识同频。那批答主里最高赞的一位，后来自己改了答案」 |
| 命中 2020 共识 | 「你和 2020 年的主流判断同频。当时这个判断成立，直到 XX 变量在 XXXX 年发生变化」 |
| 命中更早 | 「你和 2017 年的判断同频 —— 有意思的是，那一年的少数派后来赢了」 |

结果框架从「你落后 N 年」改成「**你和哪一年的知乎同频**」：中性、可分享、不伤人，而且每一种结果都带一个故事。

**验收标准**：4 个选项各自有独立的、不含贬损的结果文案；测试可重复进行不同议题。

### F6 · 传播物料（P1）

**优先做挑战链接，不是海报图。** 人传人的裂变效率远高于图传人，而且链接点击能计入使用量（人气奖按点赞 + 使用量 + 评论量评选）。

- **P1 挑战链接**：`/challenge/{topic}/{result_id}`，标题「看你卡在哪一年」，落地页直接进测试。分享者能看到自己发出的链接有多少人测过。
- **P1 体检单**：固定尺寸 DOM 截图。⚠️ 不用 Satori —— 48h 内 CJK 字体嵌入、换行、图片跨域全是坑，预计吃掉 3 小时。退化方案：设计一个正好适配手机截图比例的分享卡区块，用户自己截图；有余力再接 `html-to-image`。
- **P2 一键转发到知乎想法**：带 Tag `#挖出多年前的知乎少数派#`，引导老答主认领当年低赞回答。这条依赖站内接口，能力不确定，列为 P2 不做进主流程。

### F7 · 预生成追问按钮（P1）

砍掉 Chat 是对的，但可以保留交互深度的观感：每个议题预生成 3 个固定追问按钮，点击展示预计算内容，**0 API 消耗**。

示例：`这个变量今天还在起作用吗？` / `当年的少数派后来怎么样了？` / `同类翻转在别的议题上发生过吗？`

---

## 7. 数据模型

```typescript
type Verdict = 'verified' | 'falsified' | 'open';
type VariableType = 'cost_curve' | 'policy' | 'engineering'
                  | 'compute' | 'supply_chain' | 'demand_shift';

// 证据层：全部来自 API 原始字段或原文逐字子串，禁止模型改写
interface Evidence {
  answer_id: string;
  url: string;              // 必须可点开
  question_title: string;
  author_name: string;
  author_credential: string | null;   // 认证身份，来自 API
  created_at: string;       // ISO8601，来自 API —— 缺此字段则产品失效，见 §11
  upvote_count: number;     // 来自 API
  comment_count: number | null;
  quote: string;            // 必须满足 quote ∈ source_text（后端逐字校验）
  source_text: string;      // 检索/全文抓取得到的原文，用于校验，不出 UI
}

// 解释层：模型生成，UI 上与证据层视觉区分（灰底 + 「AI 分析」角标）
interface Interpretation {
  answer_id: string;        // 外键关联 Evidence
  stance: number;           // [-1, 1] 立场光谱得分
  stance_label: string;     // 该点立场的一句话概括
  verdict: Verdict;
  verdict_reason: string;   // 被什么事实验证/证伪
  is_minority: boolean;     // 是否属于当年的少数派簇
}

interface ConsensusFlip {
  from_paradigm: string;
  to_paradigm: string;
  flip_year: number;
  implicit_premise: string;
  changed_variable: string;
  variable_type: VariableType;
}

interface TopicGraph {
  topic: string;
  parent_topic: string;         // 归一化后的母题，缓存 key
  aliases: string[];
  axis_labels: { negative: string; positive: string };  // Y 轴两极标签
  evidences: Evidence[];
  interpretations: Interpretation[];
  flips: ConsensusFlip[];
  today_candidates: string[];   // answer_id 列表，verdict 均为 open
  quiz: {
    question: string;
    options: Array<{
      text: string;
      matches_year: number;
      matches_share: number;    // 该年份持此观点的占比，用于「84% 同频」文案
      result_copy: string;      // 每个选项独立的非贬损结果文案
    }>;
  };
  stats: {
    sample_size: number;
    accuracy_high_upvote: number;   // 万赞档事后正确率
    accuracy_low_upvote: number;    // 500 赞以下长回答正确率
  };
  curated_by_human: boolean;    // 是否经人工核验，UI 上显示徽章
  generated_at: string;
}
```

**设计要点**：`Evidence` 和 `Interpretation` 是两张表，用 `answer_id` 关联。这不只是代码整洁问题 —— 它在结构上保证了模型不可能篡改事实字段，而且让 UI 能明确区分「知乎说的」和「AI 说的」。路演时这一句话价值很高。

---

## 8. 检索管线

### 8.1 原方案的致命缺陷

原 Pipeline 写的是 `sort_by:upvotes 抓 Top 3`，紧接着说「本地提取 upvotes < 50 但评论数 > 30」。**按赞数降序取 Top 3 永远不可能落到 50 赞以下**，这两条规则互斥。而「7 赞地下先知」是整个 Demo 的心脏。搜索 API 的排序天然偏向热度和相关性，要找热度最低的那一档，不是调参能解决的，是取数方向反了。

结论：**放弃用「赞数低」定义先知。**

### 8.2 三通道检索

```
输入议题
  │
  ├─► [S0] 本地别名表归一化 → parent_topic（0 API）
  │      元宇宙 → {VR, AR, 空间计算, 头显}
  │      Sora   → {视频生成, 扩散模型, 世界模型}
  │
  ├─► 命中黄金库？ ──是──► 直接渲染（0 API，毫秒级）
  │
  └─► 冷启动：
      │
      ├─► [通道 A] 主流共识捞取（4 次搜索）
      │     每个时间切片 sort_by:upvotes 取 Top 8~10
      │     → 用于识别「当年主流簇」
      │
      ├─► [通道 B] 反共识句式定向检索（4 次搜索）★ 性价比最高
      │     query 拼接知乎特有句式：
      │       "反对最高票" + {topic}
      │       "不同意排名第一" + {topic}
      │       "等几年再来看" / "时间会证明" + {topic}
      │       "利益相关，我做这行" + {topic}
      │     → 高精度捞出非共识长答案
      │
      ├─► [通道 C] 长尾 + 今日候选（2 次搜索）
      │     sort_by:relevance / 时间序，覆盖低热度与近 6 个月
      │
      ├─► [立场聚类，直答 x1]
      │     对 A+B+C 的候选做 stance 标注 → 找出与主流簇对立的少数派簇
      │
      ├─► [全文抓取 x2~4] 只对最终入选的 2~4 条取全文
      │
      └─► [图谱生成，直答 x1] 输出 TopicGraph
```

### 8.3 通道 B 为什么是关键

「等几年再来看这个答案」是答主**自己下的赌注**，是最完美的先知素材 —— 他明确知道自己在反共识，并主动接受时间检验。

这条不只是技术手段，本身就是路演里能讲的洞察：**只有知乎有「公开反驳最高票」的文化，因此只有知乎的非共识观点是可检索的。** 这句话直接把护城河讲实了，比任何架构图都有力。

### 8.4 为什么必须抓全文

原方案砍掉全文抓取省配额，但「这条高赞回答的隐含前提是什么」需要看到论证过程，Summary 通常只有一两句。原方案的示例文案已经暴露了这个问题：「2020 年刀片/CTP 技术普及，该判断失效」是行业常识，不是从那条答案里读出来的东西 —— 说明模型当时是在用自己的知识编，而不是在读证据。

策略：**宽处扫摘要，窄处读全文**。只对最终入选的 2~4 条取全文，每次推演多花 2~4 次抓取，仍远在 1000 配额内。

---

## 9. 配额账本

### 9.1 单次冷启动推演成本

| 环节 | 搜索类 | 直答 Agent |
| :--- | :---: | :---: |
| 别名归一化（本地表） | 0 | 0 |
| 通道 A 主流共识 | 4 | — |
| 通道 B 反共识句式 | 4 | — |
| 通道 C 长尾 + 今日候选 | 2 | — |
| 全文抓取 | 2~4 | — |
| 立场聚类 | — | 1 |
| 图谱生成 | — | 1 |
| **合计** | **12~14** | **2** |

### 9.2 实际可用量（关键修正）

原方案算的是「100 次直答 = 100 次冷启动推演」。这个账不对，三个漏洞：

1. 语义溯源层需要额外调用 —— 已用本地别名表消掉（0 API）
2. JSON 校验失败重试翻倍消耗 —— 必须计入
3. 配额如果是团队共享的，开发调试 + 现场多人把玩会互相挤占

**保守设计基线：演示当天按直答仅 ~20 次可用来设计。**

| 约束 | 数字 |
| :--- | :--- |
| 搜索侧上限 | 1000 / 13 ≈ 76 次冷启动推演/天（不是瓶颈） |
| 直答侧上限 | 100 / 2 = 50 次/天（理论），~20 次（保守） |
| 黄金议题 | 15 个，命中 0 消耗，**无限次把玩** |
| 现场策略 | 主线全部走缓存；冷启动只演 1~2 次，用来证明「不是硬编码」 |

**路演现场配额纪律**：演示前 30 分钟停止一切开发调试调用，保留全部余量。

### 9.3 缓存与归一化

- 缓存 key 用 `parent_topic` 而非原始 query：「电车」「新能源车」「纯电」「电动汽车」全部命中同一条黄金数据
- 存储：SQLite 扁平 JSON（不用图数据库，原方案这条判断是对的）
- 冷启动结果同样入库，现场演过一次的议题第二次是 0 消耗

---

## 10. 内容运营：15 个黄金议题

### 10.1 议题清单

**A 档（路演主线，必须万无一失）**

| 议题 | 共识翻转 | 变量类型 |
| :--- | :--- | :--- |
| 纯电动汽车路线 | 「热失控无解，纯电是过渡」→ 「封装创新解决体积与安全」 | 工程创新 |
| 元宇宙 | 2021 狂热 → 2023 崩塌 → 2025 空间计算重构 | 需求迁移 |
| 大模型能力上限 | 「深度学习撞墙」→ Scaling Law → 后训练与 Agent | 算力阶跃 |

**B 档（备选 + 冷启动演示池）**

半导体设备国产化 / 微单取代单反 / 国产手机高端化 / 生化环材就业 / 区块链应用落地 / 直播电商 / 云原生 vs 私有部署 / Python vs Java 就业 / 短视频与长内容 / 固态电池 / 自动驾驶纯视觉 vs 激光雷达 / 考研 vs 直接就业

⚠️ 选题纪律：避开房价、政策评价等敏感议题。技术与行业类议题足够多，没必要冒风险。

### 10.2 人工核验（原排期完全遗漏，必须补上）

**这是整个项目最重要的 3~4 小时，也是原排期里唯一的 0 分配项。**

「离线跑完 15 个黄金议题并存库」不等于数据可信。可信度只能靠人逐条点开原帖核对：

- [ ] 这条回答真的存在吗？（链接可点开）
- [ ] 真的是那一年发的吗？（时间戳对得上）
- [ ] 引文真的是原文吗？（逐字比对，不是校验脚本说过就算）
- [ ] 真的被后来的事实证伪/验证了吗？（这一条最容易出错 —— 模型很爱编「后来 XX 技术普及了」）
- [ ] 答主身份没有被夸大吗？（「某车企高管」这种表述要能对上认证信息）

核过的这批数据才是真正的产品。分配：1 人 × 3~4 小时，A 档 3 个议题 100% 核验，B 档抽验 50%。

未通过核验的议题**不进演示池**，宁可只演 8 个。

---

## 11. 风险与预案

### 11.1 最高优先级：T+2h go/no-go 卡点

整个产品压在一个未验证的假设上 —— 知乎搜索 API 返回以下字段：

| 字段 | 缺失后果 |
| :--- | :--- |
| `created_at` 回答时间 | **致命**，时间轴 / 散点图 X 轴失效，产品死 |
| `upvote_count` 赞数 | **致命**，点大小 / 高低赞对比失效 |
| `url` / `answer_id` | **致命**，证据可回溯失效，可信度归零 |
| 答主认证身份 | 严重，权威度过滤降级为纯文本长度启发式 |
| 时间范围过滤 / 排序参数 | 中等，改为多取候选后本地过滤 |

**必须在写任何 UI 之前的头 1~2 小时验证完。** 原排期「0–12h 前端用假数据搭波浪图」的风险是：12 小时后发现没有时间字段，前端 12 小时全废。

**优先赛前做掉**：若 API 凭证在 09-13 之前到手，把这次探测提前到赛前 W1 完成（见 [NEXT_STEPS.md §1.1](NEXT_STEPS.md)），48h 直接以「已知走 Plan A 还是 Plan B」开局，净赚 2 小时。凭证赛前到不了手时，本节的 T+2h 卡点原样保留，不做任何删减。

**Plan B（无 `created_at` 或无赞数时立即切换）**
退化为「同一问题下的立场对撞」：X 轴改为立场光谱，Y 轴改为赞数，放弃时间维度。产品叙事从「共识如何演化」改为「同一问题下的高赞回答为何互相矛盾 + 各自的成立条件」—— 这正好回到团队原定的「答案实验室 / 成立条件 + 适用边界」方向，**F2 证据卡、F3 条件归因、F5 测试、全部前端组件、检索通道 A/B 全部可复用**，损失约 4 小时而不是 12 小时。

Plan B 触发决策权：技术负责人，T+2h 前必须给出结论并全员同步。

### 11.2 其他风险

| 风险 | 概率 | 影响 | 应对 |
| :--- | :--- | :--- | :--- |
| 模型编造引文 | 高 | 致命（评委点开发现不存在） | `quote ∈ source_text` 逐字校验 + 人工核验 + 校验失败直接丢弃不进 UI |
| 现场直答配额耗尽 | 中 | 高 | 主线走缓存；演示前 30min 冻结调试调用；本地 SQLite 全量备份 |
| JSON 输出不合法 | 中 | 中 | JSON Repair 容错模块 + 重试上限 2 次 + 重试消耗计入账本 |
| 冷启动 25s 太久 | 高 | 中 | 分段流式渲染：散点先出，卡片后补；加载动效讲「正在打捞 2014 年的回答」 |
| 散点图规律不成立 | 中 | 中 | 换议题，**不修数据**。数据造假一旦被发现是全盘否定 |
| 现场网络故障 | 中 | 高 | Localhost + 预制 SQLite 完整离线包，路演机器本地跑 |
| 答主认为被冒犯 | 低 | 中 | §4 叙事纪律：「当年的合理判断」+「变化的变量」，全文案审查一遍 |

---

## 12. 评委质询防御

**Q1：这不就是后见之明偏差吗？总结过去对预测未来有什么用？**

分三层答：
1. 产品输出的不是孤立结论，而是**共识被推翻的变量类型分布** —— 本议题 3 次翻转里工程创新占 2 次。这是可迁移的框架，用户学会的是识别「哪类变量正在被今天的共识忽略」。
2. 「条件变了」框架本身就否定了后见之明：我们不说答主错了，我们说他的隐含前提在 XXXX 年被 XX 变量打破了。这是因果分析，不是事后打分。
3. **最硬的一层是功能，不是话术** —— 指向 F4：「这是我们今天押的 3 条注，标注为待验证，生成了时间胶囊。欢迎五年后来查我们的账。」一个愿意公开留下可被证伪记录的产品，不是后见之明。

**Q2：用户搜一个才出现 1 年的新词（Sora / DeepSeek）怎么办？**

本地别名表做母题溯源，0 API 成本：Sora → 视频生成 / 扩散模型 / 世界模型 → 早期生成式图形与物理引擎讨论。追溯到 10 年技术根脉，依然有纵深。别名表覆盖不到的词，前端明确提示「该议题历史纵深不足，已切换为近 2 年视图」—— **诚实降级比强行编造安全**。

**Q3：知乎数据噪音大，怎么保证挖出的少数派不是瞎猫碰死耗子？**

三层过滤：认证身份（一线从业 / 高校科研）+ 逻辑密度（长文 + 有论据结构）+ 反共识句式（答主自己声明在反对主流）。加一句关键的：**我们不声称每个少数派都是对的** —— 散点图上黄点（未决）和红点（少数派也错了）同样展示。产品展示的是分布，不是精选正确案例。这一句会大幅提升可信度。

**Q4：32% vs 61% 这个数字有统计效力吗？**

主动承认：N=14，没有统计效力，是本议题样本的观察值，UI 上标注了样本量。这个指标的作用是引导用户点开看具体证据，不是下结论。如果要做严肃统计，需要跨议题千级样本 —— 这是产品下一步的方向，48 小时内做不到，也不装作做到了。

**Q5：为什么没有对话框？现在不都做 Agent 吗？**

三个理由：确定性（路演不能赌模型这次的输出）、配额（自由追问几分钟耗尽直答）、差异化（有输入框就是又一个 AI 问答壳）。而且我们保留了 F7 预生成追问 —— 用户要的是深度，不一定是输入框。

---

## 13. 排期（48h）

| 时段 | 前端（1 人） | 后端 / Prompt（1~2 人） | 里程碑 |
| :--- | :--- | :--- | :--- |
| **0–2h** | 项目脚手架 + 设计 token，**不写业务组件** | ★ **字段探测 + go/no-go 决策**（赛前已做完则直接进阶段 1，净赚 2h） | ★ Plan A / B 全员同步 |
| **2–12h** | 散点图组件（假数据）+ 证据卡布局 | 三通道检索封装 + `quote` 校验模块 + Schema 落地 | Pipeline 原型跑通 |
| **12–24h** | 散点↔卡片联动 + 条件归因区块 | 立场聚类 + 图谱生成 Prompt 调稳 + JSON Repair | 核心数据流打通 |
| **24–30h** | 认知年轮测试流程 | 离线跑完 15 议题入 SQLite | 离线库就绪 |
| **30–34h** | 时间胶囊页 + 挑战链接 | ★ **人工核验 15 议题**（3~4h，专人） | ★ 数据可信度确认 |
| **34–40h** | 分享卡区块 + 加载动效 + 预生成追问 | FastAPI 部署 + 缓存层 + 降级兜底 | 端到端闭环 |
| **40–44h** | UI 打磨 + 移动端适配 | 路演 PPT + 3 个冲击力案例演练 | 封版 |
| **44–47h** | Bug 修复 | 离线备份包 + 演练 3 遍 | 交付 |
| **47–48h** | 全员：最终检查清单 | | 提交 |

两处与原排期的关键差异：**0–2h 从「前端搭图」改为「字段探测」**，**30–34h 新增人工核验专项**。

---

## 14. 3 分钟路演脚本

**00:00–00:30｜护城河**
> 「现在的 AI 搜索都在回答『今天什么是对的』。但知乎真正独一无二的地方是：它记录了同一批人在 10 年里怎么一步步想错、又怎么被少数人点醒。而且 —— 只有知乎有『公开反驳最高票』的文化，所以只有在知乎，当年的非共识观点是可以被检索出来的。微博没有 10 年前的严肃长文，小红书没有跨周期复盘。」

**00:30–01:30｜散点图一击**
> 点选【纯电动汽车】，散点图展开。
> 「不用我解释，大家看这张图 —— 上半区这些又大又红的点，是当年的万赞主流判断，后来被事实推翻；下半区这些小而绿的点，是当年的少数派，后来赢了。本议题万赞档的事后正确率 32%，500 赞以下长回答 61%。」
> 点开一个红点。
> 「2016 年，1.8 万赞：三元锂五年内解决不了热失控，纯电必然是过渡。这个判断在 2016 年是**对的** —— 它的隐含前提是电池封装维度不变。2020 年 CTP 和刀片动了这个变量，结论才失效。我们不说答主错了，我们说：哪个变量变了。」
> 「而且每一条都可以点开原帖 —— 引文是原文逐字校验的，赞数和时间直接来自知乎接口，AI 只负责解释，不负责事实。」

**01:30–02:15｜今日候选先知（反后见之明）**
> 「有人会说这是事后诸葛亮。所以我们做了这个 —— 用完全相同的规则，从今天的回答里捞出 3 条低热度非共识，标注为待验证。这是我们今天押的注，生成了时间胶囊，欢迎五年后来查我们的账。」

**02:15–02:45｜拉评委入局**
> 「请评委选一个：AI Agent 的商业化瓶颈在算力还是上下文？」
> （任一选项都有好结果）「您和 2026 年知乎前沿同频 —— 这个判断在历史回答里只有 3% 提前说出来过。」

**02:45–03:00｜收尾**
> 「一次推演 2 次直答 + 12 次搜索，完成 10 年认知压缩。15 个议题已人工逐条核验。这是知乎 15 年专业资产的打捞引擎。谢谢。」

---

## 15. 评分权重对应

| 权重 | 主要得分功能 |
| :--- | :--- |
| AI 场景价值 40% | F3 条件归因（可迁移思维框架）、F4 今日候选先知（从考古到预测）、只有知乎能做的护城河论证 |
| 创新度 25% | F4 时间胶囊、F1 散点图（视觉即论点）、通道 B 反共识句式检索、无对话框形态 |
| 完成度 25% | 15 议题人工核验、引文逐字校验、Plan B、离线备份、能演 2~3 个非预设议题 |
| 体验 10% | 散点↔卡片联动、流式渲染、F5 非贬损结果文案、移动端适配 |

## 16. 明确不做

1. ❌ Chat 对话框 —— 破坏确定性、消耗配额、丢差异化
2. ❌ 全量长文抓取 —— 只对最终入选 2~4 条抓全文
3. ❌ 图数据库 —— SQLite 扁平 JSON 够用
4. ❌ 用户账号体系 —— 时间胶囊用匿名 URL
5. ❌ Satori 海报渲染 —— 48h 内 CJK 字体是坑，退化为分享卡截图
6. ❌ 跨议题统计结论 —— 样本不够，不装作够
7. ❌ 「万赞幻觉」「打脸」类贬损表述 —— 见 §4

## 17. 未决事项

| # | 事项 | 决策人 | 截止 |
| :--- | :--- | :--- | :--- |
| 1 | 搜索 API 是否返回 `created_at` / 赞数 / 认证身份 / 链接 | 技术负责人 | 赛前 W1，最迟 T+2h（阻塞） |
| 2 | 直答配额是团队共享还是个人独立 | 技术负责人 | 赛前 W1，最迟 T+2h |
| 3 | 搜索 API 是否支持时间范围过滤与排序参数 | 技术负责人 | 赛前 W1，最迟 T+2h |
| 4 | **知乎上是否真存在「高赞被证伪 + 少数派被验证」的可查案例** | 内容核验负责人 | **09-05 前（人工验证，产品地基）** |
| 5 | 赛事规则是否允许赛前写代码 | 队长 | 本周内（决定赛前准备边界） |
| 6 | 「转发到知乎想法」的站内接口是否可用 | 待确认 | T+24h（P2，可放弃） |
| 7 | A 档 3 个议题的散点图规律是否真实成立 | 内容核验负责人 | 赛前初验 + T+30h 全量核验 |
| 8 | 提交后代码能否继续修改、人气奖统计窗口 | 队长 | 09-15 前（决定阶段 C 节奏） |

第 4 条现在还是未知，而它比第 1 条更根本 —— API 字段缺失只需切 Plan B，案例不存在则主视觉的红绿规律是假的。详见 [NEXT_STEPS.md §1.1](NEXT_STEPS.md)。
