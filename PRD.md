# 《和不同》产品需求文档

> 副标题：知乎答案与人的分水岭
> 核心口号：对立的不是人，而是彼此默认的条件。

| 项 | 值 |
| :--- | :--- |
| 版本 | v3.0（卡牌局版。v2.0 的滑杆 + 条件地图交互层已废止，模型层与证据层全部保留） |
| 日期 | 2026-08-30 |
| 赛事 | 知乎黑客松 2026 校园新锐季 |
| 团队 | 2 人 —— A 产品内容、B 技术交互 |
| 开发节奏 | 08-30 起立即开工，赛前上线打磨；官方 48h 窗口（09-13 10:00 → 09-15 10:00）用于整合与提交 |
| 路演 | 2026-09-19 北京 798 |

---

## 1. 产品定义

**你是那个答主。三个人来问你同一类问题，你手上是真实的知乎高赞回答。你决定谁听哪一句。**

每个节点发 4 张卡、3 个提问者，一人一张，**必须弃掉一张**。卡面印着这条建议的适用条件——逐字来自答主原文。提问者的关键性格一开始藏着。

结算完全固定：条件对上就生效，对不上就无效，对不上又正好戳到短板就反噬。所以同一张卡打在三个人身上会得到三种结果，而每一种都能顺着卡面条件加上那个人的性格倒推回去。用户不需要人教，玩一次就懂了。

一局从 2016 走到 2026，于是还有第二种变化：一张 2016 年生效的卡，到 2022 年会失效——**不是因为人变了，是因为条件变了**。

一句话记忆点：

> **同一张卡，三个人三种结果；同一张卡，两个年代两种结果。**

核心创新是两条：**同一条建议在不同人身上后果不同**，以及**时代条件让建议失效**。两者共用一个可解释模型，且每个条件都能回溯到知乎原文。

### 1.1 为什么是这个形态（v2.0 → v3.0 的取舍）

v2.0 让用户拖 4 个滑杆描述自己。三个问题：

1. **滑杆是把模型的输入变量直接搬成了控件。** 没有家长知道自己孩子的「AI 参与程度」是 0.62，更不在乎。
2. **滑杆天然读作「测测你适合哪种」**，那是心理测试的语法，跟「答主没错，是条件变了」不是一件事。
3. **如果论点是条件变了，那证据是时间，不是用户的数值。** 让用户调自己反而把主角让错了人。

三个提问者一次就把最难讲的那句话讲完了：同一张卡，一个人变好、一个人没用、一个人更糟。这是全案最值钱的一处，所有设计都往这儿使劲。

**因此每个节点必须三人同时在场**——分开养，对比就没了。

## 2. 合并说明（给团队对齐）

本文档由两份方案合并而成。原《和不同》提供交互层与社区层，原《认知年轮》提供证据层与时间机制。

**为什么能合并**：两者本是同一机制的两种说法。认知年轮说「不是答主错了，是变量变了」；和不同 §8.5 说「不是人突然改变了价值观，而是支撑旧结论的条件发生了移动」。而和不同 §8 要求「每个时代条件必须有真实数据、历史事件或当年知乎回答作为依据，不能由 AI 凭空解释」—— 这是一张需求单，认知年轮的检索管线与逐字校验机制就是它的实现。

**合并后长出的新东西**：把历史回答当作条件空间里的**固定点**。拖动年份时，回答不动，分水岭在它们中间扫过。于是「这条回答是否已经失效」从模型断言变成几何推导 —— 分水岭在发布年到今天之间是否扫过这个点。

⚠️ 这个收益有前提：只有当「年份 → 宏观条件 → 边界位置」的映射经过人工校验，几何才是真的，否则只是把模型判断包装成坐标，反而更难被发现。真正的收益不是「更准」，而是**推理链变成可检查的**。

**取舍清单**

| 项 | 决定 | 理由 |
| :--- | :--- | :--- |
| 独立的观点时光机 / 编年史 | ❌ 废止 | 与往届《时光档案馆》高度重合。时间降为条件空间的宏观维度 |
| 共识散点图作主视觉 | ❌ 废止 | 与条件地图合并为一张图：点是回答，线是分水岭 |
| 认知年轮测试（答题） | ❌ 砍掉 | 与配卡动作重叠。评委亲手把卡配给谁比做选择题强 |
| 「万赞正确率 32% / 61%」指标 | ❌ 废止 | 在「条件变了」框架下逻辑不自洽 —— 若答主没错是世界变了，「正确率」就没有指称对象。替换为「分水岭在 11 年里移动了 3 次，2 次由工程创新推动」 |
| 多 Agent 辩论 / 胜负裁决 | ❌ 不做 | 往届已有 Agent知、讨论实验室、龙虾葩说等多个同形态项目 |
| 通用实时管线 | ❌ 不做 | 改为精选 3 议题深度人工核验，见 §4 |
| 时间层 | ✅ 保留，改名「认知年轮」作子系统 | 宏观条件层 |
| 证据可回溯 + 逐字校验 | ✅ 保留，升为全局约束 | 和不同 §17 的落地形态 |
| 条件归因（隐含前提 → 变化的变量） | ✅ 保留 | 解释分水岭为何移动的机制 |
| 今日候选先知 + 时间胶囊 | ✅ 保留，改为押注分水岭未来位置 | 从外挂功能变成条件模型的自然延伸 |
| 社区层 | ✅ 保留，改用真实答主作坐标点 | 见 §10 |

**v3.0 追加的取舍**

| 项 | 决定 | 理由 |
| :--- | :--- | :--- |
| 4 个个人条件滑杆 | ❌ 废止 | 见 §1.1。用户不该描述自己，该给别人配卡 |
| 条件地图作主交互界面 | ⬇️ 降级为复盘视图 | 几何与求解代码全部保留（见 §6.5），但它现在是局末的解释画面，不是操作面板 |
| 「微变翻转」= 拖第 4 个滑杆跨过分水岭 | ♻️ 改形态 | 变成「同一张卡打在三个人身上三种结果」。翻转仍然是核心，只是从滑杆的第 4 次微调改成配给不同的人 |
| rank → weight 映射（0.4/0.3/0.2/0.1） | ❌ 废止 | v2.0 自己写的「技术上最脆的一环」。离散卡 + 离散提问者让每个结果都能人工签字，不需要这一步（见 §6.3） |
| 运行时接 LLM 生成剧情 | ❌ 不做 | 不确定 ≠ 随机。随机结果教给用户的是「命不好」，固定结算教给用户的是「同一句话对不同人后果不同」。且随机的东西没法逐字核验，与全案立身之本冲突（见 §6.4） |
| MBTI 开局选项 | ❌ 不做，移到局末 | 「你是答主」这个框架里用户不描述自己，MBTI 没有位置。改为**由配卡逻辑反推出的类型标签**放进结局分享卡——从玩法算出来的自我标签比自陈的更站得住（见 §9.11） |
| 三个提问者是「你的三个孩子」 | ❌ 改设定 | 教育议题上，一个孩子结局不好，家长会读成在判她失败。改成「三个人来问你」——把用户放到给建议的位置，「同一句话对不同人后果不同」在那个位置上最扎心，且没有当妈的负罪感 |
| 结局打分 | ❌ 不做 | 卡牌语法的核心是赢，本案论点是没有赢家。任何给「你配对了」计分的机制都会把对错重新请回来。结局只给复盘，不给判决（见 §9.10） |

**竞品事实修正**：`hackathon.second.me` 是「SecondMe A2A 黑客松」Season 2，**已结束**（178 个项目）。原审计列的「直接竞品」全是往届已提交项目，不是本届同场对手。风险性质因此不同 —— 不是会撞车，而是**评委已经看过并评过一遍**。结论不变，理由要换。

## 3. 用户痛点

知乎上经常出现两个逻辑完整、证据充分、结论完全相反的高质量回答。常见处理是选赞数高的、让 AI 总结、或让多个 Agent 辩论。

用户真正缺的是：这两个回答分别默认了什么条件？为什么可以同时有道理？在什么条件组合下，结论会从一边翻到另一边？而且 —— 十年前成立的那些条件，今天还成立吗？

《和不同》不判断谁赢，把立场冲突还原为条件差异。

## 4. 覆盖策略：精选 3 议题做到极致

**明确不做通用实时管线。** 3 个议题的每一个条件、每一条引文、每一个宏观变量都经过人工逐条核对原帖。

这会引来「是不是硬编码 Demo」的质询，防御方式是把人工核验从成本讲成选择，并且给它配一个动作：

> 「我们不做能回答任何议题的通用工具。这 3 个议题的每一个条件都人工核对过原帖 —— 这是质量选择，不是能力上限。管线是真的，现在可以用它跑一个你们指定的新议题，60 秒出结果，但我会明确标注这是**未核验草稿**。」

诚实降级比假装通用安全，而且这套说法很难反驳。核验过的数据本身就是产品。

**议题清单**

| # | 议题 | 角色 | 时间层节点 |
| :--- | :--- | :--- | :--- |
| 1 | AI 辅助学习：促进能力还是产生依赖 | 主 Demo，演个人滑杆 | 拍照搜题(2014) → 在线教育(2017) → 疫情网课(2020) → AI 家教(2023-2026) |
| 2 | 远程办公是否可行 | 演时间层，招牌案例 | 少数派(2015) → 疫情强制(2020) → 大厂收回(2022) → 混合态(2026) |
| 3 | 纯电动汽车路线 | 演变量归因 | 骗补论(2014) → 代工堆料(2018) → CTP/刀片(2020) → 全栈出海(2026) |

⚠️ 两个待验证的前提，见 §14.1：
- 议题 1 的母题溯源（AI 辅助学习 ← 在线教育 ← 拍照搜题）在知乎上是否真有 2014~2016 年的高质量对立回答
- 议题 1~3 是否都能找到「两篇质量对等、结论相反、可查时间与赞数」的真实回答

## 5. 核心闭环

```
选议题
  ↓
见三个提问者 —— 只给表面信息（年级、成绩、可见的习惯）
  ↓
【节点 1 · 2016】发 4 张卡 = 4 篇真实知乎回答
  卡面：结论 + 适用条件（逐字来自原文）+ 答主 + 赞数 + 原帖链接
  ↓
一人配一张，弃掉一张 ★ 卡不够分是刻意的
  ↓
结算 → 生效 / 无效 / 反噬，三种结果动效不同
  ↓
翻开隐藏性格 → 「哦——因为她是这样的人」
  ↓
【节点 2 · 2019】【节点 3 · 2022】【节点 4 · 2026】
  进节点前先播卡失效回调：你上个年代给他的那张，现在还成立吗
  ↓
复盘：三条轨迹 + 条件对照 + 弃牌回收（你弃的那张本该给谁）
  ↓
条件地图视图：三人坐标 + 分水岭在 10 年里怎么扫过他们
  ↓
结局分享卡 / 押注分水岭 5 年后的位置
```

**卡为什么不够分。** 如果 4 张卡配 4 个人，用户就是在挑「哪张最好」——那又回到了对错。3 个人配 4 张卡，用户必须想「这张给谁最合适」，从判对错变成配人，正好是产品要教的事。而且会出现一个很妙的时刻：用户最喜欢的那张卡，发现谁都不合适，只能扔掉。

## 6. 结算模型

### 6.1 三个提问者：表面信息与隐藏维度

每个提问者有**可见描述**和**隐藏维度**。可见的印在卡面上，隐藏的结算后才翻开。

| | 可见（一进来就给） | 隐藏（结算后翻开） |
| :--- | :--- | :--- |
| 阿泽 | 四年级男生 · 数学 80 分 · 作业拖到十点 · 家长陪写 | 好奇心极强，**但坐不住** |
| 小满 | 初二女生 · 刷题很狠 · 从不问「为什么这么解」 | 极能坚持，**但不会自我提问** |
| 小雨 | 高一女生 · 自学能力强 · 父母打两份工基本不管 | 自控力强，**但没有任何外部支援** |

三个人对应三个维度：**自控力**（阿泽缺）、**元认知**（小满缺）、**外部资源**（小雨缺）。这三个维度正好覆盖 v2.0 那 4 个滑杆想表达的东西，但它们现在是人的属性，不是用户拖的数字。

短板必须**藏在可见描述之外**，否则没有「啊?」那一拍。「作业拖到十点」是可见的，「其实他坐不住」是隐藏的。

### 6.2 结算规则：固定，不随机

每张卡有 `requires`（这条建议依赖提问者具备什么）和 `strains`（这条建议会加重什么负担）。规则三条：

```
requires ⊆ 提问者具备的                     → 生效
requires ⊄ 提问者具备的，且 strains ∩ 短板 = ∅ → 无效
requires ⊄ 提问者具备的，且 strains ∩ 短板 ≠ ∅ → 反噬
```

举个具体的。「让他自己用 AI，家长别管」这张卡 `requires: [自控力]`，`strains: [自控力]`：

- 给阿泽（缺自控力，且这张卡正好压自控力）→ **反噬**：头几个月特别兴奋，后来不动脑子了
- 给小满（有自控力）→ **生效**：效率高了不少，但她更不问为什么了
- 给小雨（有自控力）→ **生效**

反噬是最重要的一类，「啊?」全靠它。判据是：卡的条件依赖某个能力，而这个人恰好在那个能力上有短板——于是这张卡把问题**放大**了。

**为什么必须固定：**

1. **能人工核验。** 固定的结算表可以一条条过目签字，生成的东西没法核。全案立身之本是逐字校验，这条不能松。
2. **二周目有意义。** 第二次玩你知道了性格，配法完全不同。真的重玩价值，不是假的。
3. **路演不会炸。** 现场零 AI 调用，断网也能演。

### 6.3 为什么离散卡比连续滑杆更站得住

v2.0 §6.3 自己承认 rank → weight（0.4/0.3/0.2/0.1）是「整套方案技术上最脆的一环」——那一步是人为设定的映射，不是学出来的。而连续滑杆的坐标空间里总有些角落是模型编出来的，没人核验过。

离散卡把这个脆点直接绕开：**4 张卡 × 3 个人 = 12 个格子，每个格子都能人工签字。** 权重不再需要精确到小数，只需要判断「这个人有没有这个能力」——那是个可以指着原文说是或否的判断。

代价是探索感弱了：用户不能再问「那如果他基础再好一点呢」。给每个提问者留 1~2 个「相邻画像」（换一个只差一个维度的人），**但不给回滑杆**。

### 6.4 不确定 ≠ 随机（这是全案最容易做错的一个决定）

用户会想要「每次都不一样」。做法**不是**让 LLM 现场生成，而是：

> **结算完全固定，但提问者的关键维度一开始藏着。**

同一张卡打同一个人，永远是同一个结果。意外来自**你不知道这人是什么样的人**，不来自骰子。

如果结果是随机的，用户学到的是「命不好」。如果结果来自那个人的性格，用户学到的是「同一句话对不同人后果不同」——后者才是产品论点。

**LLM 放在开发期，不放在运行时。** 想要的生成感用配额换：

- 用直答给事件文本打草稿，再人工改
- 从真实回答里抽适用条件（本来就是 §12 的活儿，配额花在这儿最值）
- 同一个结果多写 4~5 种旁白，运行时随机挑一种——用户感觉每次不一样，底下是固定的

配额花一次，所有用户共享。这是最划算的用法，而且不动「运行时 0 次 AI 调用」这条硬约束。

### 6.5 条件地图降级为复盘视图（几何代码全部保留）

`D = 适用度(A) − 适用度(B)`、`D = 0` 即分水岭、等值线按列二分求解、Catmull-Rom 平滑——`prototype/index.html` 里跑通的这些全部留用，只换了出场时机：**它不再是操作面板，是局末的解释画面。**

复盘视图上：

- 三个提问者是三个点，坐标由 §6.1 的维度算出
- 分水岭画 2016 / 2019 / 2022 / 2026 四条，或者动画扫过
- 用户看到的是：**十年里分水岭扫过了小满，没扫过小雨**

小满要刻意放在分水岭附近——「只差一点却走向另一边」那句传播语靠她成立。

### 6.6 输出措辞纪律

只能说：`这条建议的适用条件与他的情况对不上` / `在本模型中更接近某篇回答的适用区域`。
不能说：`这就是正确答案` / `概率 73%` / `你配错了` / `因果效应`。

所有数值只称「本模型中的适用度或影响贡献」。**结算结果三个词固定为「生效 / 无效 / 反噬」**，不用「成功 / 失败」——后者在给建议这个场景里是在评判用户。

## 7. 时间层「认知年轮」

### 7.1 两种「条件变了」必须都演到

| 变的是 | 表现 | 用户学到 |
| :--- | :--- | :--- |
| **人不同** | 同一张卡配给三个人 → 生效 / 无效 / 反噬 | 一条建议的对错取决于听的人是谁 |
| **年代不同** | 同一张卡在 2016 生效、2022 失效 | 一条建议的对错取决于什么年代 |

一局之内两种都演到，而且第二种更狠：**卡面一个字没改，赞数还在涨，条件已经没了。**

四个节点：**2016 · 2019 · 2022 · 2026**。节点既是提问者长大（小学中段 → 小升初 → 初二 → 高一），也是年代推进。

### 7.2 卡失效回调（时间层的主要动效）

进下一个节点前，先播一次回调：

> **2022 · 你在 2016 年给阿泽的那张卡**
> 「让他自己查答案，查不到才问人」
> 那时这条成立，因为查得到的只有题目答案。
> 现在查得到完整解题过程。
> **这张卡的条件已经不在了。**

一个节点手写 1~2 条回调就够，成本很低，但它是全案冲击最强的一处。

### 7.3 年份如何影响卡的生效

```
年份 → 宏观条件向量 → 卡的 requires 是否还成立 → 生效 / 失效
                    → 同时驱动 D=0 曲线移动（复盘视图用）
```

**不允许把年份设成一个神秘权重。** 界面必须显示年份是通过哪几个可解释的宏观条件起作用的，每个宏观条件必须挂真实依据（当年的知乎回答、公开数据或可查历史事件）。

议题 1 的宏观条件示例：工具能力（搜题→生成式）、工具可得性、教育评价方式、作业与考试形态、学校规制态度。

**版本号说法（对内的思考工具，对外文案要软化）**：「答主没错，是条件变了」→「攻略没写错，是版本变了」。没人会说一个玩家菜，因为补丁把他的配装砍了——那套配装在那个版本是对的。这个隐喻天生无罪化，而产品要的恰好就是无罪化。顺带补上 README 禁用词表的一个空缺：禁了「落后」之后一直缺替代说法，「版本变了」正好落位，而且更准确——旧攻略不是落后，是它写给的那个版本没了。

⚠️ 对家长受众「版本 / 补丁」偏硬。评委会秒懂且吃得很准；真上线的页面文案软化成「2022 年发生了什么」，结构照旧。

### 7.4 反事实：换一个人 / 换一个年代

v2.0 的「三种固定模式」改成局末两个可点的反事实：

- **换一个人** —— 这张卡如果给了小满会怎样（三格并排，一眼看到三种结果）
- **换一个年代** —— 这张卡如果在 2016 年打出会怎样

输出归因：`这次的差别主要来自 这个人的短板 / 年代条件变化 / 两者叠加`。只能称「本模型中的影响分解」，不冒充严格因果推断。

### 7.5 卡是固定点，人在时间里走

v2.0 的说法是「历史回答是地图上的固定点，分水岭扫过它们」。v3.0 把它反过来讲，更顺：

**卡（真实回答）是固定的——发布那年写下就不再变。提问者在时间里往前走。** 于是「这条回答是否已失效」= 分水岭在发布年到 2026 之间是否扫过这个人。

这比让模型直接判断「它错了吗」可辩护得多——推理链可以逐步展开给评委看，而且几何求解代码（§6.5）原样留用。

⚠️ 再强调一次：这个性质依赖 §7.3 的映射经过人工校验。精选议题满足，现场跑的新议题不满足，UI 必须标注差异。

### 7.6 招牌案例：条件变化不是单调的

```
议题 1（AI 辅助学习）
2016  拍照搜题争论，查得到的只有答案
2019  在线教育普及，工具可得性大幅上升
2022  双减 + 搜题类应用整改，往回退了一段 ★
2026  生成式工具，条件推到另一头
```

两份原始方案都默认共识单向前进（新共识取代旧共识）。**非单调直接证明产品在真的算条件，而不是在讲进步史观**，而且顺手回答一个尖锐问题：「你怎么知道今天的共识不会退回去？」—— 答案就在屏幕上，它已经退过一次了。

★ v3.0 的一个改进：非单调现在落在**议题 1 内部**（2022 双减 + 搜题应用整改），不再需要切到远程办公议题去演。这让主 Demo 一局之内就能演完两个核心创新加非单调，路演不用换议题。`prototype/index.html` 里的宏观条件锚点已经按这个调好，测试断言 `↑↑↑↑↑↑↓↑↑↑↑↑` 覆盖了这一段回退。

⚠️ 依赖 §14.1 第 5 条改版后的验证：2021~2022 搜题类应用整改与双减对「工具可得性」的真实影响，需攒 3 条以上公开依据。

---

## 8. 信息架构

### 8.1 配卡屏（主界面，一局里出现 4 次）

```
+---------------------------------------------------------------------------+
|  节点 2 / 4        2019 年        ✓ 全部引文已人工核对原帖                  |
+---------------------------------------------------------------------------+
|  三个人问了你同一类问题                                                    |
|  +-------------------+  +-------------------+  +-------------------+      |
|  |      阿泽         |  |      小满         |  |      小雨         |      |
|  |   初一 · 男       |  |   初二 · 女       |  |   高一 · 女       |      |
|  |  数学 80 分       |  |  刷题很狠         |  |  自学能力强       |      |
|  |  作业拖到十点     |  |  不问为什么       |  |  父母基本不管     |      |
|  |                   |  |                   |  |                   |      |
|  |  ┌─ 放卡处 ─┐    |  |  ┌─ 放卡处 ─┐    |  |  ┌─ 放卡处 ─┐    |      |
|  |  │          │    |  |  │  已放    │    |  |  │          │    |      |
|  |  └──────────┘    |  |  └──────────┘    |  |  └──────────┘    |      |
|  +-------------------+  +-------------------+  +-------------------+      |
+---------------------------------------------------------------------------+
|  手牌 4 张 —— 真实知乎回答，你只能放 3 张，弃 1 张                          |
|  +--------------+ +--------------+ +--------------+ +--------------+      |
|  | 让他自己查   | | 陪他一起用   | | 先自己做一遍 | | 用完让他讲   |      |
|  | 查不到才问   | | 每步都讲     | | 再对答案     | | 一遍给你听   |      |
|  |              | |              | |              | |              |      |
|  | 适用条件 ↓   | | 适用条件 ↓   | | 适用条件 ↓   | | 适用条件 ↓   |      |
|  | 「前提是孩子 | | 「我说的是   | | 「这方法只   | | 「关键在他   |      |
|  |  能坐得住」  | |  有人陪的」  | |  适合……」    | |  得会复述」  |      |
|  | ✓ 答主原文   | | ✓ 答主原文   | | ○ 人工校验   | | ✓ 答主原文   |      |
|  | @某答主 12.3k| | @某答主 8.7k | | @某答主 3.1k | | @某答主 940  |      |
|  | 🔗 原帖      | | 🔗 原帖      | | 🔗 原帖      | | 🔗 原帖      |      |
|  +--------------+ +--------------+ +--------------+ +--------------+      |
|                                                        [ 弃牌区 ]         |
+---------------------------------------------------------------------------+
```

### 8.2 结算屏（配完 3 张后）

```
+---------------------------------------------------------------------------+
|  2019 → 2022                                                              |
+---------------------------------------------------------------------------+
|  阿泽  ← 「让他自己查」                                       ⚠ 反噬       |
|    头几个月特别兴奋，后来不再自己想了。                                    |
|    这张卡的条件：「前提是孩子能坐得住」 ✓ 答主原文自述  🔗                 |
|    翻开阿泽：好奇心极强，但坐不住。 ← 这一格正好是他的短板                 |
+---------------------------------------------------------------------------+
|  小满  ← 「用完让他讲一遍」                                    ✓ 生效      |
|    她第一次被问住了，然后开始自己问为什么。                                |
+---------------------------------------------------------------------------+
|  小雨  ← 「陪他一起用，每步都讲」                              ○ 无效      |
|    没有人能陪。这条建议对她不成立 —— 不是她的问题，是条件对不上。          |
+---------------------------------------------------------------------------+
|  你弃掉了「先自己做一遍再对答案」                                          |
|    [ 看看它本该给谁 ]                                                     |
+---------------------------------------------------------------------------+
```

### 8.3 复盘屏（局末，条件地图在这里出场）

```
+---------------------------------------------------------------------------+
|  三条轨迹                          |  条件地图（分水岭扫过谁）              |
|  阿泽 2016 ✓ → 2019 ⚠ → 2022 ○   |   自主能力 ↑                          |
|  小满 2016 ○ → 2019 ✓ → 2022 ✓   |          │   ○ 小雨                   |
|  小雨 2016 ⚠ → 2019 ○ → 2022 ✓   |          │  ╲   ● 小满 ← 只差一点     |
|                                    |   ───────┼───╲────── 2016 ────       |
|  最关键的一步                      |          │     ╲  2022 ──── 2026     |
|  2019 你给阿泽的那张卡             |          │  ○ 阿泽                   |
|  条件是「能坐得住」，他坐不住      |   工具介入 →                          |
|                                    |  十年里分水岭扫过了小满，没扫过小雨    |
+------------------------------------+---------------------------------------+
|  你的配卡逻辑                                                              |
|  你 3 次依据「这个人缺什么」配卡，1 次依据「这张卡赞数最高」配卡            |
|  [ 生成结局卡 ]   [ 押注 2031 年分水岭在哪 ]                               |
+---------------------------------------------------------------------------+
```

## 9. 功能需求

### F1 · 卡组（P0）

每个节点 4 张卡 = 4 篇真实知乎回答。**不再要求「恰好两篇对立」**——4 张里通常含 2 篇明确对立 + 2 篇各有侧重，这比硬凑对立更接近知乎实际长相，也让配卡有真实的取舍。

每张卡展示：结论一句话、**适用条件（逐字，卡面主体）**、来源徽章、答主名与认证、发布时间、赞数、原帖链接。

**检索方式**：利用知乎特有的反共识句式定向找对立面 —— `"反对最高票"`、`"不同意排名第一"`、`"等几年再来看"`、`"利益相关，我做这行"`。这类回答同时给出**对立立场**和**答主自己声明的分歧点**，正好是条件提取的原料。

这一条本身就是路演里能讲的洞察：**只有知乎有「公开反驳最高票」的文化，所以只有在知乎，高质量异见是可检索的。** 微博没有 10 年前的严肃长文，小红书没有跨周期复盘。

**验收**：每个节点 4 张卡全部人工核验通过，引文逐字。

**★ 卡牌格式本身就是一个论点。** 一张万智牌的文本几乎从不是断言，而是「如果……就……」——卡牌这个格式**逼你把前提写出来**，槽位空着这张卡就不成立。散文回答的原生单位是断言：「别让孩子用 AI 搜题」，前提可以完全省略，而且省略了读起来更有力，所以高赞回答系统性地在省略前提。

**做成卡牌之后，「适用条件」不是我们额外补的注释，是卡面上必须印的一行字。** 这就是全案一直在跟平台格式作对的那件事，路演里值得单独讲一句。

另一个必须守住的区分：**答主不是卡，答主是卡牌设计师。** 你可以说一张卡在当前版本不好用，但不会说设计它的人蠢。这样批评有了落点，人还是被尊重的。

### F2 · 条件提取与可回溯（P0）

每张卡提取 1 个主适用条件（印在卡面）+ 最多 3 个次要条件（点开可见）。每个条件必须展示：

- 来源于哪篇回答的哪个段落（原文可展开）
- 对 A / B 的影响方向与权重
- 来源三档标注：**答主原文自述 / 人工校验 / AI 推测**（最高档必须能点进原帖核对）
- 当前模型的不确定性

**硬约束**：引文必须是原文逐字子串，后端做 `quote in source_text` 校验，不通过直接丢弃不进 UI。禁止润色，禁止用省略号拼接不连续片段。

### F3 · 配卡与结算（P0，核心创新一）

拖拽把卡放到提问者的放卡处；3 个放满后「弃掉最后一张」的动作才可用。结算按 §6.2 三条规则出生效 / 无效 / 反噬。

**必须做到**：任何一个节点里，**至少存在一张卡使三个提问者出三种不同结果**。这是全案最高光的 15 秒，不能靠运气——必须在数据层穷举验证，而不是只测评委大概会怎么配。

结算后翻开隐藏维度，并明写因果链：

> 这张卡的条件：「前提是孩子能坐得住」（✓ 答主原文自述）
> 阿泽：好奇心极强，但坐不住 ← 这一格正好是他的短板

**验收**：穷举 4×3 = 12 个格子，每格的结算结果和旁白都经人工签字；至少一张卡三种结果齐全；任意配法都不会出现空白旁白。

### F4 · 时间层：卡失效（P0，核心创新二）

节点推进 + 卡失效回调 + 宏观条件展示 + 依据回链 + 局末两个反事实。详见 §7。

**验收**：每个节点切换都播卡失效回调；每个宏观条件都能点开看真实依据；至少一处能演出条件往回退（非单调，见 §7.6）。

### F5 · 条件为何变了（P0）

每次卡失效 / 分水岭移动给出结构化解释：

- `implicit_premise` 旧结论的隐含前提
- `changed_variable` 实际变化的变量
- `variable_type` 变量归类：成本曲线 / 政策 / 工程创新 / 算力 / 供应链 / 需求迁移 / 社会规范
- 依据链接
- 议题级汇总：「本议题条件变化 3 次，其中工程创新推动 2 次」

这替换了原方案那个不自洽的「万赞正确率」指标 —— 同样可传播，但它是条件模型的直接产物，不需要声称统计效力，也不给答主打分。

### F6 · 三个提问者（P0，见 §10）

### F7 · 弃牌回收（P1，成本极低冲击很强）

结算屏给一个「看看它本该给谁」。如果被弃掉的那张卡对某个提问者恰好生效：

> 你弃掉的那张，正好是小满需要的。

一句话，一个按钮，纯前端。它把「卡不够分」这个机制的痛感兑现了。

### F8 · 换一个人 / 换一个年代（P1）

局末反事实。三格并排显示同一张卡给三个人的结果，或同一张卡在两个年代的结果。纯前端计算，成本极低，且直接强化两个核心记忆点。

### F9 · 押注 2031 年分水岭（P1）

原「今日候选先知 + 时间胶囊」的合并升级版。用户押注 5 年后分水岭会往哪个方向移动、由哪个宏观变量推动，留一句理由，生成 `/capsule/{id}` 永久 URL。

这是对「后见之明偏差」质询的功能性回答：**我们不只解释过去，我们留下可被证伪的记录。** 押注对象从「某条回答会不会被验证」改成「分水岭会往哪走」，逻辑上和条件模型完全一致，不再是外挂功能。

### F10 · 答主自述的边界（P0，见 §10.4）

从原回答里逐字提取答主自己声明的适用范围，标为最高可信档。**这从 P1 升为 P0** —— 它不再依赖任何人回复，成本降到和 F2 同一量级，而可信度收益是全产品最高的一处。

v3.0 里它的地位又升了一级：**答主自述的边界现在就是卡面主体。** 卡牌格式给它一个必填槽位（见 F1），所以「最高可信档」不再是一个角落里的徽章，而是这张卡打给谁的判据本身。

### F11 · 结局分享卡（P1，唯一的自传播机制）

`html-to-image` 出图。卡面放：三条轨迹缩略 + 最关键那一步 + **由配卡逻辑反推出的类型标签**。

类型标签从实际玩法算，不让用户自陈：

| 判据 | 标签 |
| :--- | :--- |
| 多次依据「这个人缺什么」配卡 | 条件派 |
| 多次依据「这张卡赞数最高」配卡 | 从众派 |
| 多次把压短板的卡避开 | 稳妥派 |
| 多次给同一个人连续下重手 | 激进派 |

这是 MBTI 那个想法的落点（见 §2 取舍表）：**从玩法算出来的自我标签，比自陈的更站得住**，而且它顺手证明了「决定结果的是处境不是性格」——两个玩家可以标签相同、三条轨迹完全不同。

### F12 · 游戏质感（P1，按性价比排）

体验分只占 10%，但这几条是「像个游戏」和「像个表单」的分界，且都很便宜：

1. **发牌手感** —— 洗牌、飞入、翻面、确认轻响。纯 CSS transform，性价比最高的一项
2. **三种结算结果动效必须不同** —— 生效上浮、无效淡出、反噬轻微抖动。不用红绿，用运动区分
3. **弃牌手势** —— 拖到弃牌区有阻尼感，松手前可以后悔
4. **卡面翻转揭示适用条件** —— 条件不是一进来就摊开，是翻面才看到
5. **提问者待机呼吸** —— 0.8Hz 两帧。什么都不做时人在动，世界就活了
6. **音效** —— 发牌 + 确认 + 结算三种，默认静音但喇叭图标放显眼。投影路演时声音是几乎免费的巨大差异
7. **节点切换像版本更新** —— 卡失效回调用等宽字排成 changelog 样式

## 10. 社区层：真实答主作坐标点

### 10.0 三个提问者是虚构的，卡是真的

v3.0 引入一个必须说清的区分：

| 层 | 真实性 | 处理 |
| :--- | :--- | :--- |
| **卡**（建议、适用条件、答主、赞数、链接） | 真实知乎回答，逐字校验 | 硬约束照旧，一字不改 |
| **三个提问者**（阿泽 / 小满 / 小雨） | **虚构画像** | UI 必须标「示例画像」，不假装是真人 |
| **结算旁白**（生效后发生了什么） | 我们写的，人工核过 | 标 `人工校验`，不标最高档 |

⚠️ 这是全案唯一一处引入虚构内容的地方，必须**主动**披露，不能等评委问。说法：「卡是真的，每张都能点进原帖；三个提问者是示例画像，因为我们不会拿真实的孩子做演示对象。」

反过来讲这甚至是加分项——如果用真实的家长提问和真实的孩子情况，那才有隐私问题。

### 10.1 为什么提问者不是「你的三个孩子」

见 §2 取舍表。核心是：教育议题上，一个孩子结局不好，家长会读成在判她失败。改成「三个人来问你」有三个收益：

1. 把用户放到**给建议的位置**——「同一句话对不同人后果不同」在那个位置上最扎心
2. 没有当妈的负罪感
3. 它就是知乎上正在发生的事：一个答主写一条回答，读它的人处境各异

### 10.2 真实答主仍然在场：卡背后的人

每张卡挂真实答主。局末复盘时可以点开：

```
这张卡的作者 @某答主 · 12.3k 赞 · 2016-03
他自己写的边界：「前提是孩子能坐得住」 ✓ 答主原文自述  🔗 原帖
```

**和的人** = 卡组里同侧、条件近、但方法不同的答主。
**不同的人** = 同愿、条件最接近、却在另一侧的答主 —— 不是立场最远的人。

传播语：**最值得认识的异见者，不是离你最远的人，而是只差一点点却走向另一边的人。**

### 10.3 位置会变（P0）

复盘视图上分水岭扫过 2016 → 2026，页面提示：

> 没有永远的同类，也没有永远的对立者；条件变了，彼此的位置也会改变。

当年的主流可能是今天的少数派——这在卡上直接可见：同一个答主的同一条回答，2016 年在分水岭这侧，2026 年在那侧。

### 10.4 答主自述的边界

最高可信度的条件不来自事后询问，而来自**答主自己在原回答里声明的适用范围**。知乎回答里这类句式很常见：

> 「前提是……」「如果……就完全不一样了」「这方法只适合……」「我说的是……的情况」「利益相关，我做这行」「等几年再来看」

这些是答主亲口划定的边界。我们只做一件事：**逐字引出来，标为 `✓ 答主原文自述`，附原帖链接。**

为什么这比事后问答主更强：

1. **零沟通成本** —— 不依赖任何人回复，不存在拿不到的风险
2. **可被评委当场验证** —— 点进原帖就能核对，私下的确认记录做不到这一点
3. **不曲解答主** —— 引用他自己写下的限定条件，是最尊重的用法

⚠️ 因此 §11 的 `SourceTier` 最高档命名为 `author_stated`（答主原文自述），不是 `author_confirmed`（答主事后确认）。这个区别是实质的：前者可验证，后者只能靠我们自称。

MVP 不做私信、群聊、实时辩论，也**不主动联系答主**。主动邀请答主补充边界是赛后演进方向（见 NEXT_STEPS）。

## 11. 数据模型

```typescript
// author_stated = 答主在原回答里自己写明的适用范围（可点原帖验证，最高档）
// human_verified = 我们人工核对原文后判定
// ai_inferred    = 模型推测，UI 必须显式标注
type SourceTier = 'author_stated' | 'human_verified' | 'ai_inferred';
type VariableType = 'cost_curve' | 'policy' | 'engineering' | 'compute'
                  | 'supply_chain' | 'demand_shift' | 'social_norm';

// 证据层：来自 API 原始字段或原文逐字子串，模型不可改写
interface Evidence {
  answer_id: string;
  url: string;
  question_title: string;
  author_name: string;
  author_credential: string | null;
  created_at: string;          // ISO8601，来自 API
  upvote_count: number;        // 来自 API
  quote: string;               // 必须满足 quote ∈ source_text
  source_text: string;         // 校验用，不出 UI
}

// 能力维度：提问者具备或缺失的东西，也是卡的 requires / strains 的取值域
// 三个维度对应三个提问者的短板（§6.1），可按议题扩展但一个议题内不超过 5 个
type Capacity = 'self_control'    // 自控力 —— 阿泽缺
              | 'metacognition'   // 元认知（会自我提问） —— 小满缺
              | 'external_support'// 外部支援（有人陪、有人管） —— 小雨缺
              | 'knowledge_base'  // 知识基础
              | 'tool_access';    // 工具可得（受年代影响，见 MacroCondition）

// 条件：每个都必须可回溯。v3.0 起它印在卡面上，不再是滑杆端点文案
interface Condition {
  id: string;
  label: string;               // 卡面显示的适用条件，人话
  quote: string;               // 逐字原文，必须满足 quote ∈ source_text
  requires: Capacity[];        // 这条建议依赖对方具备什么
  strains: Capacity[];         // 这条建议会加重什么负担（反噬的判据）
  evidence_answer_id: string;  // 来自哪篇回答
  evidence_paragraph: string;  // 哪个段落
  source_tier: SourceTier;     // UI 上必须显示
}

interface MacroCondition {
  id: string;
  label: string;               // 如「工具可得性」
  values_by_year: Record<number, number>;
  basis: Array<{              // 每个年份取值的真实依据
    year: number;
    kind: 'zhihu_answer' | 'public_data' | 'historic_event';
    ref: string;               // URL 或出处
    note: string;
  }>;
  source_tier: SourceTier;
}

interface WatershedShift {
  from_year: number;
  to_year: number;
  direction: 'toward_a' | 'toward_b';   // 允许往回退
  implicit_premise: string;
  changed_variable: string;
  variable_type: VariableType;
  basis_refs: string[];
}

interface Person {                       // 真实答主，同时是地图上的点
  answer_id: string;                     // 关联 Evidence
  coords: { x: number; y: number };
  side: 'a' | 'b';
  shared_goal: string;
  biggest_diff_condition: string;
  boundary_quote: string | null;         // 答主原文自述的适用范围，逐字
  boundary_tier: SourceTier | null;      // 有 boundary_quote 时通常为 author_stated
}

// ── v3.0 新增：卡牌局 ──────────────────────────────────────────

// 提问者：虚构示例画像，UI 必须标明（§10.0）
interface Asker {
  id: string;
  name: string;                          // 阿泽 / 小满 / 小雨
  visible: string[];                     // 一进来就给的表面信息
  has: Capacity[];                       // 具备的能力
  lacks: Capacity[];                     // 短板 —— 结算后才翻开
  reveal: string;                        // 翻开时那句话：「好奇心极强，但坐不住」
  coords_by_year: Record<number, { x: number; y: number }>;  // 复盘视图用
  is_fictional: true;                    // 常量 true，用于 UI 强制打标
}

// 卡：一张 = 一篇真实知乎回答
interface Card {
  id: string;
  node_year: number;                     // 属于哪个节点
  headline: string;                      // 结论一句话
  condition: Condition;                  // 卡面主体，含 requires / strains
  evidence_answer_id: string;            // 关联 Evidence（答主、赞数、链接、时间）
  dies_after_year: number | null;        // 条件在哪年之后不再成立，null = 未失效
}

type Outcome = 'effective' | 'ineffective' | 'backfire';  // 生效 / 无效 / 反噬

// 结算格：4 张卡 × 3 个提问者 = 12 格，每格人工签字
interface Cell {
  card_id: string;
  asker_id: string;
  outcome: Outcome;                      // 由 §6.2 规则算出，人工复核
  narration: string[];                   // 4~5 种旁白，运行时随机取一条
  because: string;                       // 因果链：哪个 requires 对不上 / 哪个 strains 撞了短板
  verified_by: string;                   // 核验人，留痕
}

// 卡失效回调：进下一节点前播（§7.2）
interface EraCallback {
  from_year: number;
  to_year: number;
  card_id: string;                       // 上个节点打出去的哪张卡
  was_valid_because: string;             // 那时为什么成立
  now_invalid_because: string;           // 现在为什么不成立
  macro_condition_id: string;            // 挂到哪个宏观条件
  basis_refs: string[];                  // 真实依据
}

interface Node {
  year: number;
  index: number;                         // 1~4
  cards: Card[];                          // 4 张
  cells: Cell[];                          // 12 格
  callbacks: EraCallback[];                // 1~2 条，节点 1 为空
}

interface Topic {
  id: string;
  title: string;
  askers: Asker[];                       // 3 个虚构画像
  nodes: Node[];                         // 4 个节点（首发 2 个，见 §17）
  evidence: Evidence[];                  // 所有卡引用的原始证据，分表
  macro_conditions: MacroCondition[];
  shifts: WatershedShift[];
  people: Person[];                      // 卡背后的真实答主
  axis_x: { label: string; capacities: Capacity[] };  // 复盘视图的合成轴
  axis_y: { label: string; capacities: Capacity[] };
  curated: boolean;                      // 人工核验徽章
}
```

**要点**：`Evidence` 与解释性字段分表，`source_tier` 是每个条件和宏观条件的必填项。这不只是代码整洁 —— 它在结构上保证模型改不了事实，且让 UI 能区分「知乎说的」「人核过的」「AI 猜的」。路演时这句话很值钱。

**v3.0 的三个结构性变化：**

1. **`Condition` 长出了 `requires` / `strains`**，丢掉了 `weight_a` / `weight_b` 和滑杆端点文案。权重从连续小数变成能力集合的交集判断——**可以指着原文说是或否**，这是 §6.3 那个脆点的解法。
2. **`Cell` 带 `verified_by`。** 12 格每格留核验人痕迹。评委问「这些结果是编的吗」，答案是一张能翻开的核验表。
3. **`Asker.is_fictional` 是常量 `true`。** 类型层面强制 UI 打标，防止虚构画像在某个组件里悄悄漏成真人（§10.0）。

## 12. 离线内容管线

3 议题的数据全部离线生产 + 人工核验，运行时**不调 LLM**。API 只在内容生产阶段用。

```
议题 → 反共识句式检索找候选回答（搜索 API ~10 次）
     → 抓 4~8 篇全文（~8 次）
     → 抽适用条件 + 判定 requires / strains（直答 ~4 次）
     → 12 格结算旁白打草稿（直答 ~4 次）★ 生成感在这里买，不在运行时
     → 宏观条件与依据检索（搜索 ~6 次）
     → 人工核验（★ 全部条目逐条过，含 12 格逐格签字）
     → 落 SQLite
```

单节点约 16 次搜索 + 8 次直答。首发 2 个节点 ≈ 32 次搜索 + 16 次直答；做满 4 节点 ≈ 64 + 32，反复迭代也远在配额内（搜索 1000/天、直答 100/天）。

⚠️ 直答用量比 v2.0 涨了一个量级（2 → 32），因为旁白草稿走这条路。仍在配额内，但要注意直答是 100/天——**做满 4 节点那一轮不要压在同一天**。

**运行时 0 API 消耗** —— 现场无论多少人把玩都不会耗尽配额，这本身是完成度的加分项。唯一的在线调用是「现场跑新议题」的那一次演示。

配额纪律：演示前 30 分钟停止一切调试调用。

## 13. 上线与埋点

**范围：公开 URL + 匿名会话（可选昵称）。**

不做密码、不做邮箱、不收集 PII，因此不触发鉴权与数据合规负担。cookie 存持久 ID，可设显示名，坐标与押注存服务端，跨设备靠一个恢复链接。拿到 90% 的价值和 20% 的成本 —— 2 人团队里技术那一个人的时间不该花在账号系统上。

- 部署：前端静态托管 + FastAPI + SQLite
- 埋点（人气奖需要使用量数据）：议题选择、每格配卡选择、弃掉哪张、节点完成率、**局末完成率**（最关键的一个数）、结局卡生成、押注提交、分享链接点击
- 分享链接把整局配卡编进 URL 参数，打开即复现对方那一局
- ⚠️ 匿名会话仍会产生用户行为数据，隐私说明要写清楚存了什么

**新增一个必看指标：节点流失。** 卡牌局比滑杆长，用户可能玩到节点 2 就走。如果节点 2 → 3 流失超过一半，说明节奏太慢，要砍到 2 个节点收尾。这个数只有真人能给，也是 09-09 早上线的主要理由。

**配卡数据本身有分析价值：** 「87% 的人把『让他自己查』给了小雨」这类分布，是站内传播的现成素材，也是赛后跨议题统计的原料。

## 14. 风险与预案

### 14.1 地基验证（★ 优先级最高，本周内完成，全部纯人工不写代码）

产品压在几个未验证的前提上。**每一条都在知乎上人工确认，不需要 API、不需要代码。**

| # | 待验证 | 不成立的后果 | 兜底 |
| :--- | :--- | :--- | :--- |
| 1 | 议题 1 每个节点凑得出 4 张「可查时间与赞数、侧重明确不同」的真实回答 | 产品没有原料，整个方案失效 | 换议题（备选：微单取代单反、健身减肥方法论） |
| 2 | AI 辅助学习的母题溯源成立 —— 2016 年前后真有拍照搜题的高质量争论 | 节点 1 撑不起来，一局只剩 3 个节点 | 起点后移到 2019，纵深从 10 年缩到 7 年 |
| 3 | 目标回答里含答主自述边界的句式（「前提是」「只适合」「利益相关」等） | **卡面主体拿不到** —— v3.0 起这是致命的，不再只是少一档徽章 | 降级：卡面条件标 `人工校验`，并在 UI 说明为何没有最高档 |
| 4 | 搜索 API 返回 `created_at` / 赞数 / 原帖链接 | 历史回答无法定位，时间层与证据层受损 | 手工录入（首发 8 张卡，可行） |
| 5 | 2021~2022 双减 + 搜题类应用整改对「工具可得性」的回退真实成立 | 非单调招牌案例失效 | 降级为单调，损失一个亮点；或切回远程办公议题演非单调 |

⚠️ **第 3 条的权重在 v3.0 里升级了。** v2.0 里答主自述边界拿不到只是少一档徽章；v3.0 里它是卡面主体、是配卡的判据。所以第 3 条从「掉亮点」变成了「掉地基」——A 的验证顺序应该把它提到第 2 位。

第 4 条比原方案宽松得多 —— 首发只要 8 张卡，就算 API 不给字段，手工录入也做得完。**这是「精选少数」策略的一个隐性收益。**

### 14.2 其他风险

| 风险 | 影响 | 应对 |
| :--- | :--- | :--- |
| 三种结果没演出来 | 致命，最高光镜头失效 | 数据层穷举 12 格，保证至少一张卡三种结果齐全；准备一键重开 |
| **卡牌语法把对错请回来** | 致命，产品论点自毁 | 结算词固定「生效 / 无效 / 反噬」，结局不打分只复盘；对手是处境不是另一个答主（§6.6、§9 F10） |
| **虚构画像被当成真人** | 高，可信度受损 | `Asker.is_fictional` 类型层强制打标；路演主动披露（§10.0） |
| 评委质疑结算是编的 | 高 | 12 格逐格 `verified_by` 留痕，可当场翻开核验表；条件逐字来自原文 |
| 被判定硬编码 Demo | 高 | §4 的说法 + 现场跑新议题出「未核验草稿」 |
| 模型编造引文 | 致命 | 逐字校验 + 人工核验，双保险；运行时 0 次 AI 调用（§6.4） |
| **一局太长，评委没耐心** | 高 | 路演只演 1 个节点 + 1 次卡失效回调 + 复盘；节点可跳过 |
| 用户玩到节点 2 就走 | 中 | 埋点看节点流失（§13），超过一半就砍到 2 节点收尾 |
| 现场网络故障 | 高 | 运行时本就 0 API；准备 Localhost 完整离线包 |
| 冒犯答主 | 中 | 不用「打脸」「幻觉」「落后」；答主是卡牌设计师不是卡（§9 F1）；统一表述为「当年的合理判断」+「后来变化的条件」 |

## 15. 评委质询防御

**Q1：这不是后见之明偏差吗？**
三层：① 输出的不是「谁错了」而是「哪类变量最常让条件失效」，这是可迁移框架；② 局末的「换一个人 / 换一个年代」让用户自己做实验，不是被动接受结论；③ F9 押注 2031 年分水岭 —— 我们留下了可被证伪的公开记录。

**Q2：结算结果是你们编的吗？**
分两半答。**卡面的适用条件不是编的** —— 逐字来自答主自己写的边界（「前提是……」「只适合……」），你现在就能点进原帖核对。**结算旁白是我们写的**，12 格每格留了核验人，这张表现在就能翻开。
**主动承认**：判定「这个人有没有这个能力」是我们的判断。但注意它比连续权重可核验得多 —— v2.0 的做法要把重要性排序映射成 0.4/0.3/0.2/0.1，那一步是人为设定的；现在只需要判断是或否，而且能指着原文说。这是我们做卡牌的一个技术理由，不只是好玩。

**Q3：只有一个议题，是不是硬编码？**
见 §4。补一句：「管线是真的，现在可以跑一个你们指定的议题，60 秒出一副卡，但我会标注这是未核验草稿 —— 因为核验才是这个产品的价值。」

**Q4：和往届《时光档案馆》有什么区别？**
它回答「过去的人怎么想」，时间轴是产品主体，主要是历史阅读。我们回答「为什么同一条建议对这个人有用、对那个人有害，以及为什么它在今天失效」。核心差异：**同一条建议在不同人身上的后果对比 + 条件解释 + 卡失效 + 用户要自己做决定**。
⚠️ 这个论证需要先人工打开 tmark.top 确认其实际功能（该站返回 403，抓不到）。若它已做条件分析，此答案要重写。

**Q5：为什么不做 Agent 辩论？**
往届已有多个同形态项目。而且辩论的目标是分胜负，我们的目标是让双方看清彼此的成立条件 —— 「和而不同」不是让一方赢。

**Q7：三个提问者是真人吗？（v3.0 新增，必须主动说，不等问）**
不是，是示例画像，UI 上标着。**卡是真的，人是示例的** —— 因为我们不会拿真实的孩子做演示对象。如果用真实家长的提问和真实孩子的情况，那才有隐私问题。

**Q8：做成卡牌是不是为了好玩？（v3.0 新增）**
不是。一张卡牌的文本几乎从不是断言，而是「如果……就……」——**卡牌这个格式逼你把前提写出来，槽位空着这张卡就不成立**。而散文回答的原生单位是断言：「别让孩子用 AI 搜题」，前提可以完全省略，省略了读起来还更有力，所以高赞回答系统性地在省略前提。
做成卡牌之后，适用条件不是我们额外补的注释，是卡面上必须印的一行字。**卡牌不是给知识加的皮肤，它是比散文更适合装知识的容器。**

**Q9：这游戏有输赢吗？（v3.0 新增）**
没有，而且是刻意的。卡牌游戏的语法核心是赢，我们的论点是没有赢家 —— 任何给「你配对了」计分的机制都会把对错重新请回来，而整个产品是为了拆掉对错建的。所以结算词是「生效 / 无效 / 反噬」不是「成功 / 失败」，结局给的是复盘不是分数。

**Q6：适用度是概率吗？**
不是。只是本模型中的适用度对比，不是真实因果概率，也不是正确率。措辞纪律见 §6.6。

## 16. 明确不做

多 Agent 圆桌 / 答主 Agent 自由辩论 / 模仿答主语言风格 / 胜负裁决 / 完整私信群聊 / 全站实时搜索 / 复杂人生画像 / 黑箱匹配度 / 无原文依据的权重 / 「双方都有道理」式空泛报告 / 独立观点编年史 / 只有词频与情绪的时间轴 / 无真实依据的「时代原因」/ 通用实时管线 / 密码与邮箱账号体系 / 人生选择类议题（避免撞往届《人生样本库》）

**v3.0 追加：** 个人条件滑杆 / 运行时 LLM 生成剧情 / 随机结算 / 结局打分或评级 / 卡牌对战与 PvP / 抽卡与稀有度掉率（稀有度只用来标信源档，不做概率）/ 卡组构筑（首发不做，赛后再说）/ MBTI 开局选项 / 拿真实孩子或真实家长提问做提问者 / 三个提问者伪装成真人

## 17. 排期（2 人，08-30 → 09-19）

赛前开发不受限（已确认）。因此排期的目标不是「在窗口内做完」，而是**让产品早一周到真人手里**，把用户反馈提前到还改得动的时候。

| 阶段 | 时间 | A 产品内容 | B 技术交互 |
| :--- | :--- | :--- | :--- |
| **地基验证** | 08-30 → 09-05 | ★ §14.1 五条人工验证（第 3 条提到第 2 位）；定三个提问者的能力矩阵 | API 字段探测；脚手架；配卡 + 结算原型（假数据） |
| **节点 1、2 闭环** | 09-06 → 09-09 | 节点 1、2 各 4 张卡核验完成；24 格结算逐格签字 | 配卡拖拽；结算三态；卡失效回调；复盘视图；**部署上线** |
| **首轮测试 + 补全** | 09-10 → 09-12 | ★ 首轮用户测试 ≥ 5 人；节点 3、4 核验 | 按反馈改节奏；结局卡；节点 3、4 接入 |
| **官方窗口** | 09-13 → 09-15 | 二轮测试 ≥ 10 人；路演稿定稿 | 提交物料；匿名会话；埋点；押注与胶囊；游戏质感 P1 |
| **传播冲量** | 09-16 → 09-18 | 分发结局卡与整局链接；站内钩子 | Bug 修复；移动端；离线备份包 |
| **决赛** | 09-19 | 掐表演练 ≥5 遍；质询对练 | 现场环境验证 |

**首发范围收窄为 2 个节点 8 张卡。** 一局能完整走通配卡 → 结算 → 卡失效回调 → 复盘，就够看懂产品；节点 3、4 是加深不是解锁。这个收窄是 v3.0 相对 v2.0 的主要成本增项换来的——12 格结算旁白每格都要人工签字，比 4 个滑杆权重贵得多。

两个关键前移：

**上线提前到 09-09**，只带 2 个节点。宁可先上一半也要早上线 —— 因为「同一张卡三种结果这一下用户看懂了吗」和「一局会不会太长」这两个问题只有真人能回答，而答案可能要求改节奏甚至砍节点。09-12 才拿到这个反馈就只能硬扛。

**首轮测试提前到 09-10**，此时节点 3、4 还没接完，改起来成本最低。

⚠️ 时间富余优先补节点 3、4，**不要加第 2 个议题**。一个议题演深比两个议题演浅强，因为卡失效回调需要时间纵深才有冲击力。

⚠️ 09-13 是报名截止日，同时是窗口首日 —— 报名务必提前完成。

⚠️ 「赛前可开发」不等于「无需说明」。仓库提交时间戳从 08-30 起公开可见，若提交时有前置工作披露要求，如实写明即可 —— 这不是风险，隐瞒才是。

## 18. 3 分钟路演脚本

**只演 1 个节点。** 4 个节点走完超过 3 分钟，而演一个节点就够看懂两个核心创新。

**00:00–00:20｜破题**
> 「知乎上最常见的不是没有答案，而是两个高质量答案完全相反。我们总以为其中一个必须是错的。但也许，他们只是默认了不同的条件——只是那句『前提是』，从来不会写在标题里。」

**00:20–01:20｜配卡，同一张卡三种结果**
> 「现在你是答主。三个人来问你同一类问题——他们是示例画像，但**这四张卡是真的知乎回答**，每张都能点进原帖。」
> 「注意卡面这一行：适用条件。这不是我们加的注释，是答主自己写的『前提是孩子能坐得住』。」
> 「请评委给三个人各配一张。四张卡三个人，**你得扔掉一张**。」
> 结算。
> 「同一张卡，阿泽反噬、小满生效、小雨无效。翻开看为什么——阿泽好奇心极强，但坐不住，而这张卡的条件正好是『能坐得住』。」
> 「**这条建议没写错。它只是不是写给他的。**」

**01:20–02:10｜时间层，卡失效**
> 「刚才变的是人。现在人不动，我只往前走六年。」
> 卡失效回调。
> 「2016 年这张卡成立，因为那时查得到的只有答案。现在查得到完整解题过程。**卡面一个字没改，赞数还在涨，条件已经没了。**」
> 「而且看这里，2022 年往回退了一段——搜题类应用整改。**共识不总是往前走的。**」

**02:10–02:40｜复盘**
> 打开条件地图：「十年里分水岭扫过了小满，没扫过小雨。」
> 「还有一件事——你刚才扔掉的那张卡，正好是小满需要的。」
> 「最值得认识的异见者，不是离你最远的人，而是只差一点点却走向另一边的人。」

**02:40–03:00｜收尾**
> 「每张卡的引文都人工核对过原帖，12 格结算每格都有核验人签字，运行时零次 AI 调用，断网也能跑。我们还留了一个 2031 年的赌注，欢迎五年后来查账。」
> 「知乎最珍贵的东西不是答案，是那些答主亲口写下的『前提是』。我们只做了一件事：把它印在卡面上。」

## 19. 评分权重对应

| 权重 | 得分点 |
| :--- | :--- |
| AI 场景价值 40% | 条件提取与归因（可迁移框架）、卡牌格式给「适用条件」一个必填槽位（Q8）、卡失效解释共识为何过期、只有知乎能做的反共识检索文化 |
| 创新度 25% | 同一张卡三种结果、卡不够分、卡失效回调、非单调条件变化、弃牌回收、由玩法反推的类型标签 |
| 完成度 25% | 逐字校验、12 格逐格签字可当场翻开、运行时 0 API、真实上线 + 用户测试、离线备份 |
| 体验 10% | 发牌手感、三种结算动效可辨、结局分享卡、移动端 |

## 20. 未决事项

| # | 事项 | 决策人 | 截止 |
| :--- | :--- | :--- | :--- |
| 1 | §14.1 五条地基验证（**第 3 条提到第 2 位**——它现在是卡面主体） | A | 09-05（阻塞） |
| 2 | tmark.top 实际功能（403 抓不到，需人工打开） | A | 09-05（决定 Q4 怎么答） |
| 3 | ~~赛事规则是否限制赛前写代码~~ | — | ✅ 已确认可提前开发 |
| 4 | 官方赛程日期核对（公开渠道查不到校园新锐季） | 队长 | 本周内 |
| 5 | 提交时是否需披露前置工作 | 队长 | 09-13 前 |
| 6 | 三个提问者的名字与画像定稿（要能一眼对号入座，又不能像真人） | A | 09-05 |
| 7 | 人气奖统计窗口 | 队长 | 09-15 前 |
| 8 | 节点数最终定 2 个还是 4 个 | A + B | 09-11（看首轮测试的节点流失） |
| 9 | 「版本 / 补丁」说法对外要不要软化 | A | 09-12（二轮测试后定） |
