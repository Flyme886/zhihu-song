# 《和不同》产品需求文档

> 副标题：知乎答案与人的分水岭
> 核心口号：对立的不是人，而是彼此默认的条件。

| 项 | 值 |
| :--- | :--- |
| 版本 | v2.0（《和不同》× 认知年轮 合并版，v1.0 独立时间机器方案已废止） |
| 日期 | 2026-08-30 |
| 赛事 | 知乎黑客松 2026 校园新锐季 |
| 团队 | 2 人 —— A 产品内容、B 技术交互 |
| 开发节奏 | 08-30 起立即开工，赛前上线打磨；官方 48h 窗口（09-13 10:00 → 09-15 10:00）用于整合与提交 |
| 路演 | 2026-09-19 北京 798 |

---

## 1. 产品定义

把同一问题下**相互对立的真实知乎回答**放进同一张条件地图。用户调整个人条件，观察自己在地图上移动、结论如何跨过分水岭；拖动年份，观察分水岭本身向自己移动。系统同时把用户连接到分水岭两侧条件最接近的真实答主。

一句话记忆点：

> **改变自己，人在地图上移动；改变时代，分水岭向你移动。**

核心创新是两条：**多条件微变驱动结论翻转**，以及**时代条件推动分水岭移动**，两者在同一个可解释模型里，且每个条件都能回溯到知乎原文。

## 2. 合并说明（给团队对齐）

本文档由两份方案合并而成。原《和不同》提供交互层与社区层，原《认知年轮》提供证据层与时间机制。

**为什么能合并**：两者本是同一机制的两种说法。认知年轮说「不是答主错了，是变量变了」；和不同 §8.5 说「不是人突然改变了价值观，而是支撑旧结论的条件发生了移动」。而和不同 §8 要求「每个时代条件必须有真实数据、历史事件或当年知乎回答作为依据，不能由 AI 凭空解释」—— 这是一张需求单，认知年轮的检索管线与逐字校验机制就是它的实现。

**合并后长出的新东西**：把历史回答当作条件空间里的**固定点**。拖动年份时，回答不动，分水岭在它们中间扫过。于是「这条回答是否已经失效」从模型断言变成几何推导 —— 分水岭在发布年到今天之间是否扫过这个点。

⚠️ 这个收益有前提：只有当「年份 → 宏观条件 → 边界位置」的映射经过人工校验，几何才是真的，否则只是把模型判断包装成坐标，反而更难被发现。真正的收益不是「更准」，而是**推理链变成可检查的**。

**取舍清单**

| 项 | 决定 | 理由 |
| :--- | :--- | :--- |
| 独立的观点时光机 / 编年史 | ❌ 废止 | 与往届《时光档案馆》高度重合。时间降为条件空间的宏观维度 |
| 共识散点图作主视觉 | ❌ 废止 | 与条件地图合并为一张图：点是回答，线是分水岭 |
| 认知年轮测试（答题） | ❌ 砍掉 | 与滑杆功能重叠。滑杆已把用户定位到坐标，再答一遍题是重复劳动；且评委亲手拖出翻转比做选择题强 |
| 「万赞正确率 32% / 61%」指标 | ❌ 废止 | 在「条件变了」框架下逻辑不自洽 —— 若答主没错是世界变了，「正确率」就没有指称对象。替换为「分水岭在 11 年里移动了 3 次，2 次由工程创新推动」 |
| 多 Agent 辩论 / 胜负裁决 | ❌ 不做 | 往届已有 Agent知、讨论实验室、龙虾葩说等多个同形态项目 |
| 通用实时管线 | ❌ 不做 | 改为精选 3 议题深度人工核验，见 §4 |
| 时间层 | ✅ 保留，改名「认知年轮」作子系统 | 宏观条件层 |
| 证据可回溯 + 逐字校验 | ✅ 保留，升为全局约束 | 和不同 §17 的落地形态 |
| 条件归因（隐含前提 → 变化的变量） | ✅ 保留 | 解释分水岭为何移动的机制 |
| 今日候选先知 + 时间胶囊 | ✅ 保留，改为押注分水岭未来位置 | 从外挂功能变成条件模型的自然延伸 |
| 社区层 | ✅ 保留，改用真实答主作坐标点 | 见 §10 |

**竞品事实修正**：`hackathon.second.me` 是「SecondMe A2A 黑客松」Season 2，**已结束**（178 个项目）。原审计列的「直接竞品」全是往届已提交项目，不是本届同场对手。风险性质因此不同 —— 不是会撞车，而是**评委已经看过并评过一遍**。结论不变，理由要换。

## 3. 用户痛点

知乎上经常出现两个逻辑完整、证据充分、结论完全相反的高质量回答。常见处理是选赞数高的、让 AI 总结、或让多个 Agent 辩论。

用户真正缺的是：这两个回答分别默认了什么条件？为什么可以同时有道理？在什么条件组合下，结论会从一边翻到另一边？而且 —— 十年前成立的那些条件，今天还成立吗？

《和不同》不判断谁赢，把立场冲突还原为条件差异。

## 4. 覆盖策略：精选 3 议题做到极致

**明确不做通用实时管线。** 3 个议题的每一个条件、每一条引文、每一个宏观变量都经过人工逐条核对原帖。

这会引来「是不是硬编码 Demo」的质询，防御方式是把人工核验从成本讲成选择，并且给它配一个动作：

> 「我们不做能回答任何议题的通用工具。这 3 个议题的每一个条件都人工核对过原帖 —— 这是质量选择，不是能力上限。管线是真的，现在可以用它跑一个你们指定的新议题，60 秒出结果，但我会明确标注这是**未核验草稿**。」

诚实降级比假装通用安全，而且这套说法很难反驳。核验过的数据本身就是产品。

**议题清单**

| # | 议题 | 角色 | 时间层节点 |
| :--- | :--- | :--- | :--- |
| 1 | AI 辅助学习：促进能力还是产生依赖 | 主 Demo，演个人滑杆 | 拍照搜题(2014) → 在线教育(2017) → 疫情网课(2020) → AI 家教(2023-2026) |
| 2 | 远程办公是否可行 | 演时间层，招牌案例 | 少数派(2015) → 疫情强制(2020) → 大厂收回(2022) → 混合态(2026) |
| 3 | 纯电动汽车路线 | 演变量归因 | 骗补论(2014) → 代工堆料(2018) → CTP/刀片(2020) → 全栈出海(2026) |

⚠️ 两个待验证的前提，见 §14.1：
- 议题 1 的母题溯源（AI 辅助学习 ← 在线教育 ← 拍照搜题）在知乎上是否真有 2014~2016 年的高质量对立回答
- 议题 1~3 是否都能找到「两篇质量对等、结论相反、可查时间与赞数」的真实回答

## 5. 核心闭环

```
选议题
  ↓
看两篇真实对立回答 + 各自成立条件（每条可回溯原文）
  ↓
设置个人条件滑杆 → 头像落在条件地图上，靠近或远离分水岭
  ↓
微调多个条件 → 前几次只是接近，某一次跨过 → 结论翻转
  ↓
拖动年份 → 个人条件不变，分水岭向自己移动
  ↓
归因分解：这次翻转来自个人变化还是时代变化
  ↓
匹配「和的人」与「最近的异见者」（真实答主）
  ↓
从对立面借一招 / 押注分水岭 5 年后的位置
```

## 6. 条件地图与适用度模型

### 6.1 显示结构

地图是 2D，但底层适用度是 1D 标量。这个区分很重要，它让模型可解释。

- **底层**：`D = 适用度(A) − 适用度(B)`，单一标量。`D = 0` 即分水岭
- **显示**：2D 地图，两条轴各由两个滑杆合成
  - 议题 1 的 X 轴「工具介入程度」= 滑杆 2（独立思考时间）+ 滑杆 3（AI 参与程度）
  - 议题 1 的 Y 轴「学习者自主能力」= 滑杆 1（知识基础）+ 滑杆 4（学习后检测）
- 分水岭 = 该 2D 空间里 `D = 0` 的那条曲线
- 用户头像在 `(x, y)`；年份改变宏观条件 → 曲线整体移动

一句话向评委解释：「两条合成轴，每条由两个滑杆合成；分水岭是两篇回答适用度相等的那条线。」

### 6.2 适用度计算

```
适用度(A) = Σ wᵢᴬ · f(cᵢ) + Σ 交互项
适用度(B) = Σ wᵢᴮ · f(cᵢ) + Σ 交互项
D = 适用度(A) − 适用度(B)
```

交互项保留 1~2 个，因为部分条件不独立起作用：

```
知识基础 × AI 代答比例      （低基础 + 高依赖，风险显著放大）
独立思考时间 × 自我检测频率  （两者同时存在才形成完整学习闭环）
```

### 6.3 权重从哪来 —— 这是整套方案技术上最脆的一环

和不同 §16 明确禁止「没有原文依据的条件权重」。3 个精选议题的权重全部人工校验，所以主线安全。但「现场跑新议题」需要一条自动规则，否则权重就是黑箱。

**规则：权重从答主自己的表述里抽排序，不让模型自由发明。**

答主写反共识时通常会自己说清什么最关键 ——「瓶颈根本不在电芯能量密度，而在 BMS 与极耳良率」这句话本身就在给权重排序。抽取步骤：

1. 定位答主的强调句式（「关键在」「根本不在…而在」「最重要的是」「决定性因素」「前提是」）
2. 得到条件重要性排序
3. 排序转权重：rank1 = 0.4 / rank2 = 0.3 / rank3 = 0.2 / rank4 = 0.1
4. 每个权重挂原文出处，UI 标注三档来源：**答主原文自述 / 人工校验 / AI 推测**

排序转权重这一步是人为设定的映射，不是从数据学出来的。UI 上不掩饰这一点。

### 6.4 输出措辞纪律

只能说：`当前条件下更接近某篇回答的适用区域`。
不能说：`这就是正确答案` / `概率 73%` / `因果效应`。

所有数值只称「本模型中的适用度或影响贡献」。

## 7. 时间层「认知年轮」

### 7.1 两种移动必须视觉可辨

| 用户动作 | 屏幕上发生什么 |
| :--- | :--- |
| 调个人滑杆 | **头像移动**，分水岭不动 |
| 拖年份滑杆 | **分水岭移动**，头像不动 |

这是产品最强的一个视觉对照。评委必须能一眼看懂两者区别 —— 头像用位移动画，分水岭用整条曲线的形变动画，两者的动效语言刻意做得不一样。

### 7.2 年份如何影响分水岭

```
年份 → 宏观条件向量 → 调整 wᵢᴬ / wᵢᴮ → D=0 曲线整体移动
```

**不允许把年份设成一个神秘权重。** 界面必须显示年份是通过哪几个可解释的宏观条件起作用的，每个宏观条件必须挂真实依据（当年的知乎回答、公开数据或可查历史事件）。

议题 1 的宏观条件示例：工具能力（搜题→生成式）、工具可得性、教育评价方式、作业与考试形态、学校规制态度。

### 7.3 招牌案例：远程办公的分水岭不是单调的

```
2015  分水岭在左  远程是少数派立场
2020  分水岭猛推向右  疫情强制全民远程
2022  分水岭往回退  大厂收回远程政策
2026  稳定在混合态
```

两份原始方案都默认分水岭单向前进（新共识取代旧共识）。**非单调分水岭直接证明产品在真的算条件，而不是在讲进步史观**，而且顺手回答一个尖锐问题：「你怎么知道今天的共识不会退回去？」—— 答案就在屏幕上，它已经退过一次了。

### 7.4 反事实固定

三种模式，让用户分离两类变量：

- 固定个人条件，只拖年份 → 时代变化如何改变结论
- 固定年份，只调个人条件 → 个人差异如何改变结论
- 两者同时变 → 真实世界的综合演化

输出归因分解：`本次翻转主要来自 宏观环境变化 / 个人条件变化 / 两者交互`。只能称「本模型中的影响分解」，不冒充严格因果推断。

### 7.5 历史回答作为固定点

每篇历史回答按其隐含条件在地图上有坐标。拖年份时回答不动，分水岭扫过它们。

于是「这条回答是否已失效」= 分水岭在发布年到 2026 之间是否扫过该点。这比让模型直接判断「它错了吗」可辩护得多 —— 推理链可以逐步展开给评委看。

⚠️ 再强调一次：这个性质依赖 §7.2 的映射经过人工校验。3 个精选议题满足，现场跑的新议题不满足，UI 必须标注差异。

---

## 8. 信息架构

```
+---------------------------------------------------------------------------+
|  议题  [AI 辅助学习]  [远程办公]  [纯电动汽车]        ✓ 3 议题已人工核验   |
+---------------------------------------------------------------------------+
|  两篇真实对立回答                                                          |
|  +---------------------------+  +---------------------------+             |
|  | A 促进学习  12.3k 赞      |  | B 削弱思考  8.7k 赞       |             |
|  | 「原文逐字引用……」        |  | 「原文逐字引用……」        |             |
|  | 🔗 原帖   适用度 62%      |  | 🔗 原帖   适用度 38%      |             |
|  +---------------------------+  +---------------------------+             |
+---------------------------------------------------------------------------+
|  条件地图                                    |  个人条件                   |
|                                              |                            |
|   自主能力 ↑                                 |  知识基础                   |
|          │      A 适用区                     |  零基础 ──●──── 熟练        |
|          │    ● 你                           |                            |
|          │   ╲                               |  独立思考时间               |
|   ───────┼────╲──── 分水岭 ────────          |  立即问 ────●── 充分        |
|          │      ╲                            |                            |
|          │        ╲   B 适用区               |  AI 参与程度                |
|          │                                   |  代答 ──●───── 只反馈       |
|   工具介入 →                                 |                            |
|                                              |  学习后检测                 |
|  ○ 历史回答（固定点）  ● 你  ━ 分水岭        |  无 ──────●─── 主动回忆     |
|                                              |                            |
|  年份 ──────●────────  2020                  |  距分水岭：0.08（很近）      |
|  2014      2020      2026                    |  [固定个人] [固定年份]      |
+---------------------------------------------------------------------------+
|  分水岭为何移动                     |  地图上的人                          |
|  2020：疫情改变「工具可得性」和     |  和的人（同侧·条件近·方法不同）      |
|  「学校规制态度」两个宏观条件        |    @某答主 12.3k 赞  🔗              |
|  依据：当年知乎回答 + 教育部通知     |  最近的异见者（另一侧·距离最小）      |
|  [查看依据]                         |    @某答主 340 赞  差 3 个条件  🔗   |
|                                     |    ✓ 答主原文自述：「前提是……」🔗   |
+-------------------------------------+--------------------------------------+
|  从对立面借一招  |  最小变化组合  |  押注 2031 年分水岭在哪            |
+---------------------------------------------------------------------------+
```

## 9. 功能需求

### F1 · 对立回答对（P0）

两篇真实、质量对等、结论相反的知乎回答，各自展示：答主名与认证身份、发布时间、赞数、逐字引文、原帖链接、当前适用度百分比。

**检索方式**：利用知乎特有的反共识句式定向找对立面 —— `"反对最高票"`、`"不同意排名第一"`、`"等几年再来看"`、`"利益相关，我做这行"`。这类回答同时给出**对立立场**和**答主自己声明的分歧点**，正好是条件提取的原料。

这一条本身就是路演里能讲的洞察：**只有知乎有「公开反驳最高票」的文化，所以只有在知乎，高质量异见是可检索的。** 微博没有 10 年前的严肃长文，小红书没有跨周期复盘。

**验收**：3 议题各有 1 组对立回答，全部人工核验通过。

### F2 · 条件提取与可回溯（P0）

每篇回答提取 4 个条件（最多不超过 5 个）。每个条件必须展示：

- 来源于哪篇回答的哪个段落（原文可展开）
- 对 A / B 的影响方向与权重
- 来源三档标注：**答主原文自述 / 人工校验 / AI 推测**（最高档必须能点进原帖核对）
- 当前模型的不确定性

**硬约束**：引文必须是原文逐字子串，后端做 `quote in source_text` 校验，不通过直接丢弃不进 UI。禁止润色，禁止用省略号拼接不连续片段。

### F3 · 个人条件滑杆与微变翻转（P0，核心创新一）

4 个滑杆（议题 1）：知识基础 / 独立思考时间 / AI 参与程度 / 学习后检测。

**必须做到**：前 3 次微调只让头像逼近分水岭，第 4 次跨过并触发翻转动画。跨越瞬间显示：

> 没有任何单个条件足以改变结果，是 4 个微小变化共同推动了结论翻转。

初始值要**预先调校到离分水岭很近但没跨过**，保证现场稳定触发。这是路演最高光的 15 秒，不能靠运气。

**验收**：任意评委按任意顺序拖动 4 个滑杆各一小步，翻转必然发生（先验证参数空间，别只测一条路径）。

### F4 · 时间层：分水岭移动（P0，核心创新二）

年份滑杆 + 宏观条件展示 + 依据回链 + 反事实固定三模式 + 归因分解。详见 §7。

**验收**：拖年份时头像**不动**、分水岭动，两种动效语言明显不同；每个宏观条件都能点开看真实依据；远程办公议题能演出分水岭往回退。

### F5 · 分水岭为何移动（P0）

每次分水岭移动给出结构化解释：

- `implicit_premise` 旧结论的隐含前提
- `changed_variable` 实际变化的变量
- `variable_type` 变量归类：成本曲线 / 政策 / 工程创新 / 算力 / 供应链 / 需求迁移 / 社会规范
- 依据链接
- 议题级汇总：「本议题分水岭移动 3 次，其中工程创新 2 次」

这替换了原方案那个不自洽的「万赞正确率」指标 —— 同样可传播，但它是条件模型的直接产物，不需要声称统计效力，也不给答主打分。

### F6 · 地图上的人（P0，见 §10）

### F7 · 从对立面借一招（P1）

不折中、不改变方向，让每条路径吸收对方一个优点。例如效率派吸收「先独立思考再问 AI」「要求 AI 只给提示」；谨慎派吸收「用 AI 做即时反馈」「用 AI 生成分层练习」。

输出不是「双方都有道理」，而是：**不改变方向，但因为理解了对立面而修正路径。**

### F8 · 最小变化组合（P1）

「若要跨到另一侧，最少需要改变哪些条件组合」。纯前端计算，成本极低，且能强化「积微成变」的记忆点。

### F9 · 押注 2031 年分水岭（P1）

原「今日候选先知 + 时间胶囊」的合并升级版。用户押注 5 年后分水岭会往哪个方向移动、由哪个宏观变量推动，留一句理由，生成 `/capsule/{id}` 永久 URL。

这是对「后见之明偏差」质询的功能性回答：**我们不只解释过去，我们留下可被证伪的记录。** 押注对象从「某条回答会不会被验证」改成「分水岭会往哪走」，逻辑上和条件模型完全一致，不再是外挂功能。

### F10 · 答主自述的边界（P0，见 §10.4）

从原回答里逐字提取答主自己声明的适用范围，标为最高可信档。**这从 P1 升为 P0** —— 它不再依赖任何人回复，成本降到和 F2 同一量级，而可信度收益是全产品最高的一处。

## 10. 社区层：真实答主作坐标点

### 10.1 为什么不用预置虚构用户

原方案用 4~6 个预置社区用户。改用真实答主，三个收益：

1. 真人真回答，不造假档案，符合知乎原生
2. 历史回答点和社区用户卡**是同一张表** —— 数据结构统一，前端少一套组件
3. 「最近的异见者」变成真实可点击的答主，说服力完全不同

### 10.2 匹配逻辑

```
共同目标一致（如都想真正提高学习能力）
+ 条件空间距离尽可能小
+ 位于分水岭另一侧
+ 回答质量足够高
= 最近的异见者
```

**和的人** = 同侧、条件近、但方法不同的答主。
**不同的人** = 同愿、条件最接近、却在另一侧的答主 —— 不是立场最远的人。

传播语：**最值得认识的异见者，不是离你最远的人，而是只差一点点却走向另一边的人。**

### 10.3 人物动态重排（P0）

拖动滑杆后，原来的「和的人」可能变成异见者，原来的异见者可能变得更近。页面提示：

> 没有永远的同类，也没有永远的对立者；条件变了，彼此的位置也会改变。

拖年份时人物也重排 —— 当年的主流可能是今天的少数派。

### 10.4 答主自述的边界

最高可信度的条件不来自事后询问，而来自**答主自己在原回答里声明的适用范围**。知乎回答里这类句式很常见：

> 「前提是……」「如果……就完全不一样了」「这方法只适合……」「我说的是……的情况」「利益相关，我做这行」「等几年再来看」

这些是答主亲口划定的边界。我们只做一件事：**逐字引出来，标为 `✓ 答主原文自述`，附原帖链接。**

为什么这比事后问答主更强：

1. **零沟通成本** —— 不依赖任何人回复，不存在拿不到的风险
2. **可被评委当场验证** —— 点进原帖就能核对，私下的确认记录做不到这一点
3. **不曲解答主** —— 引用他自己写下的限定条件，是最尊重的用法

⚠️ 因此 §11 的 `SourceTier` 最高档命名为 `author_stated`（答主原文自述），不是 `author_confirmed`（答主事后确认）。这个区别是实质的：前者可验证，后者只能靠我们自称。

MVP 不做私信、群聊、实时辩论，也**不主动联系答主**。主动邀请答主补充边界是赛后演进方向（见 NEXT_STEPS）。

## 11. 数据模型

```typescript
// author_stated = 答主在原回答里自己写明的适用范围（可点原帖验证，最高档）
// human_verified = 我们人工核对原文后判定
// ai_inferred    = 模型推测，UI 必须显式标注
type SourceTier = 'author_stated' | 'human_verified' | 'ai_inferred';
type VariableType = 'cost_curve' | 'policy' | 'engineering' | 'compute'
                  | 'supply_chain' | 'demand_shift' | 'social_norm';

// 证据层：来自 API 原始字段或原文逐字子串，模型不可改写
interface Evidence {
  answer_id: string;
  url: string;
  question_title: string;
  author_name: string;
  author_credential: string | null;
  created_at: string;          // ISO8601，来自 API
  upvote_count: number;        // 来自 API
  quote: string;               // 必须满足 quote ∈ source_text
  source_text: string;         // 校验用，不出 UI
}

// 条件：每个都必须可回溯
interface Condition {
  id: string;
  label: string;
  axis_low: string;            // 滑杆左端文案
  axis_high: string;           // 滑杆右端文案
  weight_a: number;            // 对回答 A 的影响
  weight_b: number;
  evidence_answer_id: string;  // 来自哪篇回答
  evidence_paragraph: string;  // 哪个段落
  source_tier: SourceTier;     // UI 上必须显示
}

interface MacroCondition {
  id: string;
  label: string;               // 如「工具可得性」
  values_by_year: Record<number, number>;
  basis: Array<{              // 每个年份取值的真实依据
    year: number;
    kind: 'zhihu_answer' | 'public_data' | 'historic_event';
    ref: string;               // URL 或出处
    note: string;
  }>;
  source_tier: SourceTier;
}

interface WatershedShift {
  from_year: number;
  to_year: number;
  direction: 'toward_a' | 'toward_b';   // 允许往回退
  implicit_premise: string;
  changed_variable: string;
  variable_type: VariableType;
  basis_refs: string[];
}

interface Person {                       // 真实答主，同时是地图上的点
  answer_id: string;                     // 关联 Evidence
  coords: { x: number; y: number };
  side: 'a' | 'b';
  shared_goal: string;
  biggest_diff_condition: string;
  boundary_quote: string | null;         // 答主原文自述的适用范围，逐字
  boundary_tier: SourceTier | null;      // 有 boundary_quote 时通常为 author_stated
}

interface Topic {
  id: string;
  title: string;
  answer_a: Evidence;
  answer_b: Evidence;
  conditions: Condition[];               // 4 个，最多 5
  interactions: Array<{ cond_a: string; cond_b: string; weight: number }>;
  macro_conditions: MacroCondition[];
  shifts: WatershedShift[];
  people: Person[];
  axis_x: { label: string; conditions: string[] };   // 合成轴
  axis_y: { label: string; conditions: string[] };
  curated: boolean;                      // 人工核验徽章
}
```

**要点**：`Evidence` 与解释性字段分表，`source_tier` 是每个条件和宏观条件的必填项。这不只是代码整洁 —— 它在结构上保证模型改不了事实，且让 UI 能区分「知乎说的」「人核过的」「AI 猜的」。路演时这句话很值钱。

## 12. 离线内容管线

3 议题的数据全部离线生产 + 人工核验，运行时**不调 LLM**。API 只在内容生产阶段用。

```
议题 → 反共识句式检索找对立回答对（搜索 API ~8 次）
     → 抓两篇全文（~2 次）
     → 条件提取 + 权重排序（直答 ~2 次）
     → 宏观条件与依据检索（搜索 ~6 次）
     → 人工核验（★ 全部条目逐条过）
     → 落 SQLite
```

单议题约 16 次搜索 + 2 次直答。3 议题一轮 ≈ 50 次搜索 + 6 次直答，反复迭代也远在配额内（搜索 1000/天、直答 100/天）。

**运行时 0 API 消耗** —— 现场无论多少人把玩都不会耗尽配额，这本身是完成度的加分项。唯一的在线调用是「现场跑新议题」的那一次演示。

配额纪律：演示前 30 分钟停止一切调试调用。

## 13. 上线与埋点

**范围：公开 URL + 匿名会话（可选昵称）。**

不做密码、不做邮箱、不收集 PII，因此不触发鉴权与数据合规负担。cookie 存持久 ID，可设显示名，坐标与押注存服务端，跨设备靠一个恢复链接。拿到 90% 的价值和 20% 的成本 —— 2 人团队里技术那一个人的时间不该花在账号系统上。

- 部署：前端静态托管 + FastAPI + SQLite
- 埋点（人气奖需要使用量数据）：议题选择、滑杆交互次数、翻转触发、年份拖动、押注提交、分享链接点击
- 分享链接把滑杆位置编进 URL 参数，打开即复现对方的坐标
- ⚠️ 匿名会话仍会产生用户行为数据，隐私说明要写清楚存了什么

## 14. 风险与预案

### 14.1 地基验证（★ 优先级最高，本周内完成，全部纯人工不写代码）

产品压在几个未验证的前提上。**每一条都在知乎上人工确认，不需要 API、不需要代码。**

| # | 待验证 | 不成立的后果 | 兜底 |
| :--- | :--- | :--- | :--- |
| 1 | 3 议题各有一组「质量对等、结论相反、可查时间与赞数」的真实回答 | 产品没有原料，整个方案失效 | 换议题（备选：微单取代单反、健身减肥方法论） |
| 2 | AI 辅助学习的母题溯源成立 —— 2014~2016 年真有拍照搜题的高质量争论 | 议题 1 时间层撑不起 10 年纵深 | 议题 1 只演个人滑杆，时间层全靠远程办公 |
| 3 | 3 议题的目标回答里含答主自述边界的句式（「前提是」「只适合」「利益相关」等） | 最高可信档拿不到，只剩人工校验与 AI 推测两档 | 降级为两档标注，并在 UI 说明为何没有最高档 |
| 4 | 搜索 API 返回 `created_at` / 赞数 / 原帖链接 | 历史回答无法定位，时间层与证据层受损 | 手工录入 3 议题的关键回答（只有 3 个议题，可行） |
| 5 | 远程办公的分水岭非单调真实成立 | 招牌案例失效 | 降级为单调移动，损失一个亮点 |

第 4 条比原方案宽松得多 —— 精选 3 议题后，就算 API 不给字段，手工录入也做得完。**这是「精选少数议题」策略的一个隐性收益。**

### 14.2 其他风险

| 风险 | 影响 | 应对 |
| :--- | :--- | :--- |
| 现场翻转没触发 | 致命，最高光镜头失效 | 初始值预调校到分水岭附近；穷举验证参数空间；准备一键复位 |
| 评委质疑权重是黑箱 | 高 | §6.3 权重从原文抽 + 三档来源标注 + 主动承认排序转权重是人为映射 |
| 被判定硬编码 Demo | 高 | §4 的说法 + 现场跑新议题出「未核验草稿」 |
| 模型编造引文 | 致命 | 逐字校验 + 人工核验，双保险 |
| 两种移动分不清 | 中 | 头像位移 vs 曲线形变，动效语言刻意不同；加一次引导动画 |
| 现场网络故障 | 高 | 运行时本就 0 API；准备 Localhost 完整离线包 |
| 冒犯答主 | 中 | 不用「打脸」「幻觉」「落后」；统一表述为「当年的合理判断」+「后来变化的条件」 |

## 15. 评委质询防御

**Q1：这不是后见之明偏差吗？**
三层：① 输出的不是「谁错了」而是「哪类变量最常推动分水岭移动」，这是可迁移框架；② 反事实固定让用户自己做实验，不是被动接受结论；③ F9 押注 2031 年分水岭 —— 我们留下了可被证伪的公开记录。

**Q2：权重是你们编的吗？**
主线 3 议题全部人工校验，每个权重挂原文出处，UI 标三档来源。最高档是**答主自己在回答里写明的适用范围** —— 那不是我们的判断，是他亲口划的边界，你现在就能点进原帖核对。自动规则是从答主自己的强调句式抽排序（§6.3）。**主动承认**：排序转权重那一步是人为设定的映射，不是学出来的 —— 所以我们标注了来源等级，让你能判断每个数字该信几分。

**Q3：只有 3 个议题，是不是硬编码？**
见 §4。补一句：「管线是真的，现在可以跑一个你们指定的议题，60 秒出结果，但我会标注这是未核验草稿 —— 因为核验才是这个产品的价值。」

**Q4：和往届《时光档案馆》有什么区别？**
它回答「过去的人怎么想」，时间轴是产品主体，主要是历史阅读。我们回答「为什么同一结论在今天失效」，时间只是条件空间的宏观维度，且可以固定变量做反事实实验。核心差异：**时间回放 + 条件解释 + 反事实固定 + 人物重排**。
⚠️ 这个论证需要先人工打开 tmark.top 确认其实际功能（该站返回 403，抓不到）。若它已做条件分析，此答案要重写。

**Q5：为什么不做 Agent 辩论？**
往届已有多个同形态项目。而且辩论的目标是分胜负，我们的目标是让双方看清彼此的成立条件 —— 「和而不同」不是让一方赢。

**Q6：适用度百分比是概率吗？**
不是。只是本模型中两篇回答的适用度对比，不是真实因果概率，也不是正确率。措辞纪律见 §6.4。

## 16. 明确不做

多 Agent 圆桌 / 答主 Agent 自由辩论 / 模仿答主语言风格 / 胜负裁决 / 完整私信群聊 / 全站实时搜索 / 复杂人生画像 / 黑箱匹配度 / 无原文依据的权重 / 「双方都有道理」式空泛报告 / 独立观点编年史 / 只有词频与情绪的时间轴 / 无真实依据的「时代原因」/ 通用实时管线 / 密码与邮箱账号体系 / 人生选择类议题（避免撞往届《人生样本库》）

## 17. 排期（2 人，08-30 → 09-19）

赛前开发不受限（已确认）。因此排期的目标不是「在窗口内做完」，而是**让产品早一周到真人手里**，把用户反馈提前到还改得动的时候。

| 阶段 | 时间 | A 产品内容 | B 技术交互 |
| :--- | :--- | :--- | :--- |
| **地基验证** | 08-30 → 09-05 | ★ §14.1 五条全部人工验证，出结论 | API 字段探测；脚手架；条件地图原型（假数据） |
| **议题 1 闭环** | 09-06 → 09-09 | 议题 1 条件与宏观条件提取核验完成 | 适用度模型；滑杆联动；微变翻转；年份层；**部署上线** |
| **首轮测试 + 补全** | 09-10 → 09-12 | ★ 首轮用户测试 ≥ 5 人；议题 2、3 核验 | 按反馈改结构；人物层；议题 2、3 接入 |
| **官方窗口** | 09-13 → 09-15 | 二轮测试 ≥ 10 人；路演稿定稿 | 提交物料；匿名会话；埋点；押注与胶囊；P1 功能 |
| **传播冲量** | 09-16 → 09-18 | 分发坐标链接；站内钩子 | Bug 修复；移动端；离线备份包 |
| **决赛** | 09-19 | 掐表演练 ≥5 遍；质询对练 | 现场环境验证 |

两个关键前移：

**上线提前到 09-09**，只带议题 1。宁可先上一个议题也要早上线 —— 因为「用户能不能看懂两种移动的区别」这个问题只有真人能回答，而它的答案可能要求改动效甚至改布局。09-12 才拿到这个反馈就只能硬扛。

**首轮测试提前到 09-10**，此时议题 2、3 还没接完，改起来成本最低。

⚠️ 时间富余不要用来加第 4 个议题。完成度占 25%，多一个议题是线性收益，早一周的用户反馈是结构性收益。

⚠️ 09-13 是报名截止日，同时是窗口首日 —— 报名务必提前完成。

⚠️ 「赛前可开发」不等于「无需说明」。仓库提交时间戳从 08-30 起公开可见，若提交时有前置工作披露要求，如实写明即可 —— 这不是风险，隐瞒才是。

## 18. 3 分钟路演脚本

**00:00–00:25｜破题**
> 「知乎上最常见的不是没有答案，而是两个高质量答案完全相反。我们总以为其中一个必须是错的。但也许，他们只是默认了不同的条件。」

**00:25–01:15｜个人滑杆微变翻转**
> 展示两篇真实对立回答 → 点开条件 → 「每个条件都来自原文，可以点开看，还标了是答主自己写的、我们人工核的，还是 AI 推的。」
> 「请评委把这四个滑杆分别拖一小步。」
> 前三次逼近，第四次翻转。
> 「没有任何一个条件单独决定结果，是四个微小变化共同把结论推过了分水岭。」

**01:15–02:00｜时间层**
> 「刚才是改变自己。现在个人条件完全不动，我只拖年份。」
> 分水岭移动。
> 「注意 —— 头像没动，是分水岭向人移动了。2020 年疫情改变了工具可得性和学校规制两个条件。而且看这里，2022 年它往回退了一段：大厂收回远程政策。**共识不总是往前走的。**」

**02:00–02:35｜连接人**
> 「我们不只重新排列答案，也重新连接人。这位是和你在分水岭另一侧、条件最接近的真实答主 —— 你们只差三个条件。最值得认识的异见者，不是离你最远的人，而是只差一点点却走向另一边的人。」
> 点开他的边界引文：「这位答主自己在回答里写过『前提是……』—— 这不是我们推测的适用范围，是他亲口划的。你现在就可以点进原帖核对。」

**02:35–03:00｜收尾**
> 「三个议题的每个条件都人工核对过原帖，运行时零 AI 调用。我们还留了一个 2031 年的赌注 —— 押分水岭会往哪走，欢迎五年后来查账。知乎不只让不同观点被看见，也让这些只差一点点的人真正相遇。」

## 19. 评分权重对应

| 权重 | 得分点 |
| :--- | :--- |
| AI 场景价值 40% | 条件提取与归因（可迁移框架）、时间层解释共识为何失效、只有知乎能做的反共识检索文化 |
| 创新度 25% | 微变翻转、分水岭移动、历史回答作固定点、非单调分水岭、最近的异见者 |
| 完成度 25% | 3 议题全人工核验、逐字校验、运行时 0 API、真实上线 + 用户测试、离线备份 |
| 体验 10% | 两种移动的动效区分、人物动态重排、可分享坐标 URL、移动端 |

## 20. 未决事项

| # | 事项 | 决策人 | 截止 |
| :--- | :--- | :--- | :--- |
| 1 | §14.1 五条地基验证 | A | 09-05（阻塞） |
| 2 | tmark.top 实际功能（403 抓不到，需人工打开） | A | 09-05（决定 Q4 怎么答） |
| 3 | ~~赛事规则是否限制赛前写代码~~ | — | ✅ 已确认可提前开发 |
| 4 | 官方赛程日期核对（公开渠道查不到校园新锐季） | 队长 | 本周内 |
| 5 | 提交时是否需披露前置工作 | 队长 | 09-13 前 |
| 6 | 议题 2、3 的合成轴怎么定 | A + B | 09-08 |
| 7 | 人气奖统计窗口 | 队长 | 09-15 前 |
